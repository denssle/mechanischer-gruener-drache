{
  "name": "mechanischer-gruener-drache",
  "version": "1.47.1",
  "description": "",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "start-server": "npm ci && npm run build && npm start",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "build:scripts": "tsc -p tsconfig.scripts.json",
    "test:twitch": "npm run build:scripts && node dist-scripts/run-integration-test.js",
    "test:modul": "npm run build:scripts && node dist-scripts/test-modul-api.js"
  },
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "discord.js": "^14.26.4",
    "express": "^5.2.1",
    "redis": "^6.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/express": "^5.0.6",
    "@vitest/coverage-v8": "^4.1.10",
    "eslint": "^10.5.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.61.0",
    "vitest": "^4.1.8"
  },
  "overrides": {
    "undici": "^6.27.0"
  }
}
