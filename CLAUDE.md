# Mechanischer Grüner Drache

Discord-Bot für den Community-Server von [Legend of the Green Dragon (LotGD)](https://www.lotgd.de/), einem Browser-RPG. Hobby-Projekt für einen kleinen, privaten Server – Features wie Ping-Pong, Sport-Tracking und Twitch-Benachrichtigungen sind Community-Spielereien für diesen Server, nicht Teil des eigentlichen Spiels.

## Techstack

- TypeScript, Node.js 20, discord.js v14, Redis (Unix Socket)
- Hosted on Uberspace, Supervisor für Prozessverwaltung

## Struktur

- src/commands/ – Slash Command Definitionen
- src/handlers/ – Business Logik
- src/services/ – Redis und externe APIs
- src/server/ – Webhook Server (Twitch EventSub)
- src/types/ – Interfaces und Types

## Konventionen

- Deutsche Bot-Antworten
- **Emoji-Sparsamkeit (seit 2026-07-07, User-Feedback „visuelles Äquivalent von ADHS"):** Bot-Antworten sind bewusst (fast) emojifrei – keine führenden Status-Icons (✅/❌/⚠️), keine dekorativen/Fließtext-Emojis, auch die Zufalls-Flavor-Zeilen (Ping-Pong/Event/Sport) tragen keine mehr. Beim Bauen neuer Antworten also **keine Emojis reflexartig einstreuen**. Die früher erlaubten `/hilfe`- und Gruppen-`hilfe`-Kategorie-Icons wurden am 2026-07-10 auf User-Wunsch ebenfalls **entfernt** – die Hilfetexte (`hilfe.handler.ts` `HELP_TEXT` sowie `📖 **…-Befehle**` in `sport`/`twitch`/`event.handler.ts`) sind jetzt emojifrei. Verbliebene bewusste Ausnahmen: die Sport-Aktivitäts-Labels (`SportActivities`, dienen als Item-Icons), die Bestätigungs-Reaktion der Sport-Auto-Erfassung (`BESTAETIGUNGS_REAKTION` = ✅, ist dort die *einzige* Rückmeldung, kein Schmuck), und die **funktionalen** Statusmarker im Audit-Log (`logging.handler.ts`: 🗑️/➕/➖/🔇/🔊 zum schnellen Scannen im Log-Channel) sowie in `/diagnose` (✅/⚠️/❌ pro Einstellung/Subscription – reines Admin-Debugging). `console.log/warn`-Log-Ausgaben sind egal (nicht User-sichtbar).
- **Command-, Subcommand- und Options-Namen sind deutsch und sprechend** (z.B. `/twitch verknuepfen`, `/sport eintragen`, Option `benutzername`/`kanal`/`beschriftung`). Umlaute in den Namen selbst vermeiden – als `ae/oe/ue` schreiben (Discord mag keine Umlaute in Command-Namen); in den `setDescription`-Texten sind Umlaute normal. Am 2026-07-04 wurde der komplette Command-Bestand von gemischt englisch/deutsch auf durchgehend deutsch umgestellt (u.a. `ping`→`pingpong`, `pinghighscore`→`pingbestenliste`, `log`→`protokoll`, `twitch set/remove/info/notification-*`→`verknuepfen/entfernen/status/benachrichtigungs*`, `sport hinzufuegen/legacy`→`eintragen/altkilometer`). `version` blieb bewusst. Die Handler-Methodennamen wurden mitgezogen (z.B. `handleSet`→`handleVerknuepfen`), damit Dispatch und Namen konsistent bleiben.
- Command-Definitionen sind untypisierte Objektliteralle (`export default { data, execute }`), keine `satisfies Command` o.ä. Der `Command`-Typ (`src/types/discord.ts`) existiert nur für `client.commands`/`interaction.handler.ts`.
- Redis-Keys immer als KEYS-Objekt im Service. **Zwei dokumentierte Legacy-Ausnahmen** (Format bewusst nicht geändert, sonst verwaisen Bestandsdaten): der Ping-Pong-Score (`userId + REDIS_KEYS.PING_PONG`) und `user.service.ts` (speichert StoredUser unter der **rohen Discord-User-ID** ohne Prefix).
- **LotGD-Scraping immer über `lotgd.service.ts` → `fetchLotgdHtml(url, kontext)`** (seit 2026-07-10, Konsistenz-Durchgang): kapselt Bot-User-Agent (Standard-Fetch-UA bekommt `403`) + ISO-8859-1-Dekodierung + Fehler-Logging an einer Stelle. `news`/`online`/`character.service` nutzen ihn; neue LotGD-Features ebenso – den Fetch **nicht** erneut im Service ausschreiben. Netzwerkfehler reicht der Helper durch, die Services fangen sie im eigenen try/catch (Muster: `null` statt Crash).
- deferReply() bei API-Calls die länger dauern können
- Ephemere Antworten über `flags: MessageFlags.Ephemeral`, **nicht** das seit discord.js v14 deprecatete `ephemeral: true` (schrieb bei jeder Nutzung eine Deprecation-Warnung ins persistierte Log; am 2026-07-04 projektweit umgestellt).
- Tests mit Vitest, Redis wird pro Testdatei inline mit `vi.mock('../services/redis.service.js', () => ({ default: { ... } }))` gemockt (kein zentrales Mock-File, `src/mocks/` existiert nicht). Braucht die Mock-Factory eine Variable, `vi.hoisted()` benutzen (die Factory wird an den Dateianfang gehoben, normale Top-Level-`const`s existieren dort noch nicht). Coverage: `npm run test:coverage` (`@vitest/coverage-v8`; `coverage.include` in `vitest.config.ts` sorgt dafür, dass auch nie importierte Quelldateien mit 0 % auftauchen statt unsichtbar zu bleiben).
- **Jeder Command hat einen `*.command.test.ts`** (seit Konsistenz-Durchgang 2026-07-10): flache Commands einen Delegations-Test (execute → Handler-Methode), Gruppen-Commands zusätzlich den **Drift-Test** (jeder im `SlashCommandBuilder` definierte Subcommand dispatcht auch im `switch` – die Namen stehen doppelt im Code, der Test ist die einzige Absicherung).

## Deployment

- `console.log`/`console.error`-Ausgaben werden vom Hoster (Uberspace/Supervisor) tatsächlich mitgeschnitten und persistiert – bei Fehlern die nur geloggt werden (z.B. gescheiterte Notification-Zustellung), ist das also keine reine Diagnose-Attrappe, sondern real nachvollziehbar. Kein zusätzliches Alerting nötig, solange man bereit ist, ins Log zu schauen. **Kehrseite (2026-07-19 bereinigt):** Weil die Logs persistiert werden, dürfen dort **keine Nachrichteninhalte** landen – der alte `message.handler.ts` loggte jede Chat-Nachricht samt Inhalt per `console.log` und hat damit die Datenschutz-Linie aus `docs/datenhaltung.md` („ohne `/protokoll` kein Nachrichteninhalt") unterlaufen; er wurde ersatzlos entfernt (war ohnehin nur ein Debug-Überbleibsel ohne Funktion). Beim Bauen neuer Message-Listener also kein pauschales Content-Logging einführen.
- GitHub Actions (`.github/workflows/deploy.yml`) deployt bei jedem Push auf `main` **direkt in Produktion** auf Uberspace, kein Staging. `npm test` → `npm run build` → Twitch-Webhook-Integrationstest gegen den echten laufenden `dist/index.js` → `rsync` → `supervisorctl restart drache`. Ein rotes Gate verhindert das Deployment, ist also bewusst streng zu behandeln, nicht nur "CI grün bekommen".
- **Node-Version: CI und Uberspace-Host müssen zusammen wandern.** Die CI (`deploy.yml`, `setup-node`) läuft seit 2026-07-09 auf Node 22, vorher 20 (seit April 2026 End-of-Life). Weil der Host sich beim Restart selbst neu baut (siehe nächster Punkt), testet die CI nur dann wirklich das, was in Produktion läuft, wenn dort dieselbe Major-Version aktiv ist – auf Uberspace umstellbar per `uberspace tools version use node 22` (danach `supervisorctl restart drache`). Driftet das auseinander, ist das Gate grün, obwohl der Host-Build scheitern kann. Seit 2026-07-19 zusätzlich abgesichert: `package.json` hat `"engines": {"node": ">=22"}` und die `.npmrc` setzt `engine-strict=true` – ein `npm ci` auf einem zu alten Node (z.B. Host noch auf 20) bricht damit **hart** ab, statt nur zu warnen. Obergrenze bewusst offen (lokal läuft Node 24).
- **Zwei unabhängige „Node 20 ist deprecated"-Baustellen, nicht verwechseln:** (1) `node-version:` bei `setup-node` – die Version, mit der der *Bot* gebaut/getestet wird, muss zum Host passen (siehe oben). (2) Die Laufzeit der *Actions selbst* – `actions/checkout@v4`/`setup-node@v4` sind intern Node-20-Actions; GitHub zwingt sie auf Node 24 und schreibt eine Annotation ins Run-Log. Das behebt **nur** ein Bump auf `@v5` (seit 2026-07-09 gesetzt), nicht die `node-version`. Ein Bump von (1) macht die Warnung aus (2) also nicht weg.
- **Nicht auf Node 24 heben, solange Uberspace es nicht anbietet.** Die Obergrenze gibt der Host vor, nicht der Node-Release-Zyklus: `uberspace tools version use node 24` scheitert mit „Node.js version 24 does not exist", höchste verfügbare Version ist 22 (Stand 2026-07-09, prüfbar per `uberspace tools version list node`). 22 ist Maintenance LTS bis April 2027 – der nächste Bump wird also erst dann fällig bzw. sobald Uberspace 24 nachzieht.
- Supervisor auf dem Uberspace-Host startet den Bot über `npm run start-server` (= `npm ci && npm run build && npm start`) – der Server baut sich also bei jedem Restart selbst nochmal aus dem gerade gerynchten Source neu, das per CI vorgebaute `dist/` aus dem Workflow wird dabei überschrieben. D.h. `npm run build` muss nicht nur in der CI, sondern auch mit dem Node/npm auf Uberspace funktionieren.
- Skripte unter `scripts/*.ts` werden für CI/lokale Nutzung mit `tsconfig.scripts.json` zu `dist-scripts/` kompiliert und mit purem `node` ausgeführt (`npm run build:scripts`, `npm run test:twitch`). **Kein ts-node** – dessen ESM-Loader-Registrierung (`--esm`/`--transpile-only`) ist zwischen Node-Versionen/npx-Installationen unzuverlässig; hat lokal auf Node 24 funktioniert, in der Node-20-CI aber mit `ERR_UNKNOWN_FILE_EXTENSION` zuverlässig gecrasht.
- **Der Supervisor-Config von `drache` (`~/etc/services.d/drache.ini` auf dem Host) braucht `stopasgroup=true` + `killasgroup=true`.** Ohne das killt `supervisorctl restart` nur den `npm`-Wrapper (`npm ci && build && npm start`), der eigentliche `node dist/index.js`-Enkelprozess überlebt verwaist und **hält Port 3000 weiter belegt**. Der neu startende Prozess bekommt dann keinen sauberen Listener auf 3000 → der Webhook-Server ist von außen nicht erreichbar (`uberspace web backend list` zeigt `/twitch http:3000 => NOT OK, no service`, extern nginx `502`) → **Twitch-EventSub-Verifizierung scheitert, Subscriptions bleiben `webhook_callback_verification_pending`, es kommt gar keine Live-Meldung an**. Am 2026-07-04 live so gewesen (Testuser bekamen nichts), per sauberem Neustart (`supervisorctl stop` → `pkill -f dist/index.js` → `start`) behoben, `killasgroup` verhindert die Wiederkehr. Diagnose-Werkzeug dafür: `/diagnose` (siehe Diagnose-Feature, zählt die EventSub-Subscriptions nach Status) + `curl -i https://enzlor.uber.space/twitch/eventsub` (gesund = `404 Cannot GET`, kaputt = `502`).
- `webhookServer.start()` registriert seit 2026-07-04 einen `server.on('error', …)`-Handler (`handleServerError`), damit ein fehlgeschlagenes Port-Bind **laut ins (persistierte) Log** schreibt statt lautlos zu verschwinden – vorher lief der Bot bei EADDRINUSE scheinbar normal weiter (Discord ok), nur der Webhook war tot, was die Ursachensuche massiv erschwert hat. Analog zur node-redis-`error`-Falle (siehe Stolperfallen).

## Bekannte Stolperfallen

- Callbacks/Handler, die als `void`-Funktion registriert werden (z.B. `webhookServer.onNotification(...)`/`onRevocation(...)` in `src/index.ts`) aber async sind, brauchen ein explizites `.catch()`. Ohne das killt eine unhandled promise rejection den kompletten Bot-Prozess (Node ≥15) – ist im Twitch-Notification-Pfad am 2026-07-03 live passiert (Redis war nicht verbunden, `handleStreamOnline` warf, Prozess crashte). **Seit 2026-07-26 haben ALLE `client.on(...)`-Registrierungen in `index.ts` ein `.catch()`** – vorher fehlte es bei den acht Logging-Events (`MessageDelete`/`MessageUpdate`/`GuildMemberAdd`/`-Remove`/`-Update`/`GuildBanAdd`/`-Remove`/`MessageBulkDelete`). Das war kein aktiver Bug (bei jedem Logging-Handler ist `try {` die erste Anweisung, es entkam nichts), aber es hielt durch Disziplin *im Handler* statt durch die Registrierung: eine Zeile vor dem `try` oder ein neuer Handler ohne try hätte den Prozess gekillt. Und die Uneinheitlichkeit (6 mit, 8 ohne) machte für einen Leser unkenntlich, welche Konvention gilt. **Regel: `.catch()` an jede Registrierung, ausnahmslos** – unabhängig davon, ob der Handler intern fängt.
- **`vitest.config.ts` grenzt die Suite auf `include: ['src/**/*.test.ts']` ein – das ist nicht kosmetisch.** Ohne die Datei sammelt vitest lokal **auch die nach `dist/` kompilierten Tests** ein (nach einem `npm run build` liegt dort von jeder `*.test.ts` eine `*.test.js`) und fährt die Suite **doppelt**: einmal gegen den Arbeitsstand, einmal gegen den Stand des letzten Builds. Man testet dann zur Hälfte alten Code, und die gemeldete Testzahl ist doppelt (am 2026-07-11 aufgefallen: „808 Tests", real sind es 409 in 41 Dateien). **In der CI fällt das nie auf**, weil dort `npm test` *vor* `npm run build` läuft und `dist/` noch leer ist – das Gate ist also grün, während lokal etwas anderes gemessen wird. Die Config nicht entfernen; kommt je ein zweites Ausgabeverzeichnis dazu (z.B. `dist-scripts/`), muss `include` weiterhin nur den Quellbaum umfassen. **Die Wurzel ist seit 2026-07-19 zusätzlich behoben:** `tsconfig.json` excluded `**/*.test.ts`, nach `dist/` werden also gar keine Tests mehr kompiliert – die vitest-Eingrenzung bleibt trotzdem als zweite Verteidigungslinie stehen (und alte `dist/`-Stände können noch Test-Reste enthalten). Kehrseite des Excludes: Tests werden von `npm run build` nicht mehr type-gecheckt, dafür gibt es `npm run typecheck` (siehe nächster Punkt).
- Type-only Imports (z.B. `import { Command } from './types/discord.js'`) werden von `vitest`/esbuild nicht auf Existenz geprüft, nur von `tsc`. Ein fehlendes Type-File fällt also nicht bei `npm test` auf. Seit 2026-07-19 gibt es dafür `npm run typecheck` (`tsc -p tsconfig.typecheck.json`, `noEmit`, **inklusive** der Testdateien) als eigenen CI-Step – nötig, weil `npm run build` die Tests seitdem gar nicht mehr sieht (`**/*.test.ts` ist im Build-`tsconfig.json` excluded, damit keine `*.test.js` mehr nach `dist/` und damit in Produktion wandern). Typfehler in Tests fallen also **nur** über `npm run typecheck` auf, nicht über `npm run build`.
- Twitch-EventSub-Notifications werden nicht per Message-ID dedupliziert – retried Twitch eine Zustellung (z.B. bei Timeout), könnte theoretisch zweimal "ist live"-gepostet werden. Bewusst nicht gefixt (Hobby-Projekt, seltener Edge-Case), falls es doch mal stört: Redis-SET mit kurzer TTL über die Message-ID wäre der Ansatz.
- Jeder node-redis-Client **muss** einen `.on('error', ...)`-Listener haben, sonst crasht ein unhandled `'error'`-Event (z.B. bei einem Verbindungsabbruch/gescheiterten Reconnect) sofort den kompletten Bot-Prozess – node-redis emittiert das aktiv, auch während der eingebauten automatischen Reconnect-Strategie. War in `redis.service.ts` bis 2026-07-03 nicht gesetzt; seitdem im Constructor registriert (nur Logging, kein Reconnect-Code nötig – node-redis regelt das selbst). Betraf potenziell den gesamten Bot, nicht nur Twitch.
- `redisService.getSortedSet()` ist **hart auf die Top 10** begrenzt (`zRangeWithScores(key, 0, 9, {REV: true})`) – gedacht für Leaderboard-Anzeigen (Ping-Pong-Highscore), nicht zum Aufsummieren über alle Mitglieder. Für "alle Werte summieren" (z.B. Sport-Gesamtkilometer) `getSortedSetAll()` benutzen (ungekürzt, `0, -1`). War in `sportService.getGesamtKilometer` bis 2026-07-03 falsch (undercounted ab >10 Score-Haltern, inkl. dem Legacy-Dummy-User).
- **Handler, die über `commands/index.js` erreichbar sind** (also von einer `*.command.ts` importiert werden, z.B. `logging.handler.ts` via `log.command.ts`), dürfen `client` **nicht auf Modul-Top-Level verwenden** (z.B. `client.on(...)` direkt als Seiteneffekt beim Import). Zirkuläre Kette `client.ts → commands/index.js → *.command.ts → handler.ts → client.ts`: `client` ist zu dem Zeitpunkt noch nicht initialisiert → `ReferenceError: Cannot access 'client' before initialization`. `twitch.handler.ts` nutzt `client` nur innerhalb von Methodenkörpern (funktioniert), `member.handler.ts` verkabelt sich selbst am Modul-Top-Level (funktioniert nur, weil es *nicht* über `commands/index.js` eingebunden wird, sondern direkt von `index.ts`). Handler, die von einem Command importiert werden UND `client.on(...)` brauchen, müssen die Verkabelung stattdessen in `index.ts` machen (wie `logging.handler.ts`). Am 2026-07-03 beim Bau des Logging-Features live so gecrasht, erst durch den Boot-Test in CI-Modus aufgefallen.

## Twitch-Feature (`/twitch`)

- 1 Discord-User = 1 Twitch-Link (`handleSet` blockt bei bestehendem Link). Channel + Rolle für Notifications sind global vom Admin gesetzt (`TWITCH:NOTIFICATION_CHANNEL`/`_ROLE`), nicht pro User.
- `twitchService.unsubscribeFromStreamOnline` behandelt ein 404 von Twitch (Subscription dort schon weg) als Erfolg – wichtig für Idempotenz bei `/twitch entfernen` und beim Revocation-Handling.
- Twitch `revocation`-Webhooks (z.B. Auth entzogen) räumen automatisch den zugehörigen Redis-Link auf (`twitchHandler.handleSubscriptionRevoked`, verkabelt über `webhookServer.onRevocation` in `src/index.ts`) – der User müsste sich danach neu verknüpfen, es bleibt aber kein verwaister Link liegen.
- Benachrichtigungskanal + -rolle werden seit 2026-07-25 über `/config` gesetzt (nicht mehr per `/twitch benachrichtigungskanal`/`benachrichtigungsrolle` – entfernt). Die Kanalwahl dort ist auf Text-/Announcement-Kanäle beschränkt (`holeTextKanaele`), damit kein Voice-Channel gewählt wird, an dem `channel.send()` still scheitern würde.
- `twitch.command.test.ts` prüft zusätzlich, dass jeder im `SlashCommandBuilder` definierte Subcommand-Name auch im `switch` in `execute()` dispatcht wird (die Namen stehen doppelt im Code, das ist die einzige Absicherung gegen Drift zwischen beiden).
- Das Twitch-Debugging steckt seit 2026-07-21 im feature-übergreifenden **`/diagnose`** (siehe Diagnose-Feature) – der frühere `/twitch diagnose`-Subcommand samt `twitchHandler.handleDiagnose` wurde **ersatzlos entfernt** (nicht parallel behalten). Der EventSub-Status-Check (nur `enabled` stellt zu) lebt jetzt in `diagnose.handler.ts` weiter; die frühere End-to-End-**Testnachricht in den Kanal fiel bewusst weg** (auf User-Wunsch – eine allgemeine Diagnose soll nicht mehrere Kanäle vollposten, die `client.channels.fetch`-Abrufbarkeitsprüfung reicht).
- `handleStreamOnline` loggt seit 2026-07-04 an **allen** Abbruchstellen (unbekannter Broadcaster / kein Kanal / Kanal nicht abrufbar) mit `console.warn` – vorher waren zwei davon stille `return`s, was „keine Meldung"-Fehler unnötig schwer nachvollziehbar machte (Hoster persistiert die Logs).
- Die Live-Meldung nennt seit 2026-07-05 zusätzlich **Stream-Titel + Spiel/Kategorie**. Das `stream.online`-Event liefert die **nicht** mit (nur Broadcaster + `started_at`), daher ein separater Helix-`Get Streams`-Call (`twitchService.getStreamInfo(userId)` → `/streams?user_id=`). Bewusst **fehlertolerant**: `getStreamInfo` fängt alles ab und gibt `null` zurück, die Meldung baut Titel/Spiel als **optionale Zeilen** – kommt nichts zurück (Abruf-Fehler *oder* direkt beim Live-Gehen noch keine Daten bei Twitch), fällt sie einfach auf die alte Form zurück statt zu blockieren.

## Sport-Feature (`/sport`)

- **Admin-Funktionen laufen seit 2026-07-25 über `/config`** (Kilometerstand eines Mitglieds setzen, Bestandskilometer, Meilenstein anzeigen/entfernen; Ankündigungskanal) – die entsprechenden Slash-Commands (`setzen`/`altkilometer`/`altkilometer-setzen`/`ankuendigungskanal`/`meilenstein liste`/`entfernen`) sind **entfernt**, ihre Handler-Methoden ebenso. Die **Service-Methoden bleiben** (`setKilometer`/`addLegacyKilometer`/`setLegacyKilometer`/`removeMilestone`/`setAnnouncementChannel` – von `config.settings.ts` genutzt). Als Commands verbleiben die User-Aktionen (`eintragen`/`loeschen`/`bearbeiten`/`statistik`/`gesamt`/`hilfe`) und `meilenstein setzen` (bewusst für alle offen). Details zur Seite: Abschnitt „Web-Konfigurationsseite". Command-Erwähnungen weiter unten in diesem Abschnitt beschreiben teils das Verhalten (unverändert), nicht mehr den Zugangsweg.
- Bewusst **kooperativ, nicht kompetitiv**: alle tragen zu einer gemeinsamen Gesamt-Kilometerzahl bei (`/sport gesamt`), es gibt keine Rangliste. Ein früherer `/sport bestenliste`-Befehl (Top-10-Leaderboard) wurde deshalb wieder entfernt (samt `sportService.getHighscore`) – nicht versehentlich, das war eine bewusste Design-Entscheidung gegen Konkurrenzdenken auf dem Server.
- `/sport eintragen` zeigt seit 2026-07-05 direkt in der Bestätigung die **neue gemeinsame Gesamtdistanz** (`getGesamtKilometer()` gleich mit abgefragt) – unterstreicht das kooperative Design (jeder Eintrag zahlt sichtbar auf die Gruppensumme ein), auf User-Wunsch.
- Die Eintrags-Bestätigung ist seit 2026-07-07 ein **Embed statt reinem Text** (auf User-Wunsch „persönlicher, damit man sich im Post wiederfindet"): `setAuthor` mit `interaction.user.displayName` + `displayAvatarURL()` (Name + Profilbild oben). Die **Eintrags-ID wanderte in den Footer** (bleibt für `/sport loeschen`/`bearbeiten` sichtbar). `handleEintragen` antwortet damit als einziger Sport-Handler mit `{embeds: [...]}` statt String – Tests prüfen entsprechend `reply.embeds[0].toJSON()`.
- **Auto-Erfassung im Sport-Kanal (seit 2026-07-13, Vorbild Blåhaj-Listener):** Schreibt jemand im Sport-Kanal eine normale Nachricht mit **`+`-markierter** Kilometer-Angabe („+12 km gelaufen"), trägt `sportHandler.handleMessage` (`MessageCreate`, eigene Registrierung in `index.ts` mit `.catch()`) den Eintrag automatisch ein und **quittiert nur per Reaktion** (`message.react(BESTAETIGUNGS_REAKTION)`, seit 2026-07-14 – siehe „Bestätigungen sind privat" unten). Vier bewusste Unterschiede zum Blåhaj-Listener: (1) **nur im konfigurierten Kanal** – serverweit würde jedes „noch 3 km bis zum Bahnhof" die gemeinsame Gesamtdistanz verfälschen; der Kanal ist bewusst **derselbe wie der Meilenstein-Ankündigungskanal** (`SPORT:ANNOUNCEMENT_CHANNEL`, gesetzt per `/sport ankuendigungskanal`) – kein zweiter Kanal-Befehl, ohne konfigurierten Kanal passiert gar nichts. (2) `parseKilometer` nimmt **nur die erste** km-Angabe (Blåhaj summiert alle) – doppelt gezählte Distanz würde die Gesamtsumme dauerhaft verfälschen – und verlangt seit **2026-07-14 zwingend ein `+`** direkt vor der Zahl (`/\+\s*(\d…)\s*(?:km|kilometer)\b/`, User-Wunsch): auch im Sport-Kanal wird über Kilometer *geredet* („die Strecke sind 12 km"), der Eintrag soll eine bewusste Geste sein. Ohne `+` passiert nichts. **Regression 2026-07-14 bis 2026-07-26:** `HELP_TEXT` warb weiter mit „12 km gelaufen" (ohne `+`), während `/sport hilfe` bereits korrekt war – wer der Übersicht folgte, bekam gar keine Reaktion und musste den Bot für kaputt halten. Der Sport-Hilfetext ist deshalb jetzt als **`SPORT_HILFE`** exportiert (wie `CHARAKTER_HELP`/`SPIELWELT_HELP`), und ein Test in `sport.handler.test.ts` zieht **alle in „…" zitierten km-Beispiele aus beiden Hilfetexten** und lässt sie durch den echten `parseKilometer` laufen. Ändert sich der Parser, fällt eine Hilfe auf, die etwas verspricht, was er nicht einlöst. (3) `erkenneAktivitaet` rät die Sportart per Schlüsselwort, **ohne Treffer `DEFAULT_AKTIVITAET` = Laufen**. Die Muster sind **auf Wortgrenze verankert** (`\b`), nicht `includes`: „rad" steckt sonst in „Grad", „gerad" in „gerade" → beides würde als Radfahren eingetragen (Test nagelt das fest). (4) Bot-Nachrichten ignorieren ist auch hier **Pflicht** – die frühere Text-Bestätigung enthielt „12 km" und hätte sich selbst getriggert; der Check bleibt als Schutz stehen, falls je wieder geantwortet wird.
- **Auch bearbeitete Nachrichten werden erfasst (seit 2026-07-14):** `handleMessageUpdate` (eigene `MessageUpdate`-Registrierung in `index.ts` neben der des Loggings) läuft durch denselben Kern `erfasseKilometerAusNachricht` wie `handleMessage`. Grund: seit das `+` Pflicht ist, ist „Marker vergessen, Nachricht nachträglich korrigiert" der wahrscheinlichste Weg zum Eintrag. **Die Gefahr ist der Doppel-Eintrag** – Discord feuert `MessageUpdate` bei *jeder* Bearbeitung, `+5 km` → `+5 km, war schön` würde die 5 km sonst ein zweites Mal eintragen und die gemeinsame Gesamtdistanz dauerhaft verfälschen. Schutz ist die **eigene ✅-Reaktion** (`hatBereitsQuittung` → `reactions.cache.get(…)?.me === true`; `me` = *der Bot* hat reagiert, eine ✅ eines Mitglieds zählt nicht): hängt sie dran, wurde schon erfasst. Bewusst **kein zusätzlicher Redis-State** – die Quittung, die der Bot ohnehin setzt, *ist* die Markierung. Partielle Nachrichten (nicht gecacht) werden per `fetch()` nachgeladen, sonst wären `content` und `reactions` leer. Bewusst hingenommen: `+5 km` nachträglich auf `+50 km` zu ändern trägt **nicht** nach (dafür `/sport bearbeiten`), und wer die Reaktion von Hand entfernt und erneut bearbeitet, kann doppelt eintragen – das muss man absichtlich tun.
- **Bestätigungen sind privat (seit 2026-07-14, User-Wunsch „nur die eintragende Person soll sie sehen"):** `/sport eintragen` antwortet mit `flags: MessageFlags.Ephemeral` (das Embed bleibt, nur eben ephemer). Die **Auto-Erfassung kann das prinzipiell nicht** – ephemere Nachrichten hängen bei Discord an einem **Interaction-Token**, und eine normale Chat-Nachricht ist keine Interaction (dieselbe Mechanik, warum die Tipps nur an Slash-Commands hängen können). Deshalb dort statt einer Antwort nur eine **Reaktion** auf die Nachricht – der geräuschloseste Weg, der bleibt; die neue Gesamtdistanz nennt sie dafür nicht mehr (dafür gibt es `/sport gesamt`). Verworfen: Bestätigung per DM (kommt bei geschlossenen DMs gar nicht an). Der **Meilenstein-Post ist damit der einzige Sport-Post, den alle im Kanal sehen** – das ist gewollt, er ist der gemeinsame Moment.
- **Die Eintrags-ID ist aus der Bedienung verschwunden (seit 2026-07-13):** `/sport bearbeiten kilometer:<n>` und `/sport loeschen` (ganz ohne Option) arbeiten **immer auf dem zuletzt eingetragenen Eintrag** des Users (`sportService.editLastEntry`/`deleteLastEntry` → letztes Element der `SPORT:USER:<id>`-Liste, die per `rPush` gefüllt wird, also das neueste; `null` = User hat nichts eingetragen). Grund: eine UUID abzutippen war den Aufwand nie wert – korrigiert/gelöscht wird praktisch immer der eben gemachte Eintrag. `editEntry(userId, entryId, km)`/`deleteEntry` bleiben als Basis bestehen (die `*LastEntry`-Methoden benutzen sie). Folge: die **Eintrags-ID wird nirgends mehr angezeigt** – der Footer des Eintrags-Embeds ist weg, ebenso die ID in der Auto-Erfassungs-Antwort. Wird je wieder ein Zugriff auf ältere Einträge gebraucht, wäre eine Auswahl (Select-Menü/Buttons) der Weg, **nicht** die Rückkehr der UUID. `deleteLastEntry` gibt den gelöschten Eintrag zurück (nicht nur `boolean`), damit die Bestätigung nennen kann, was tatsächlich weg ist.
- **Kürze vor Flavor (seit 2026-07-13, User-Wunsch „so kurz und knapp wie möglich"):** Die Beschreibung im Eintrags-Embed ist **eine Zeile** (`Laufen – **10 km**, gemeinsam schon **250 km**.`), `/sport gesamt` ebenfalls (ohne `**Gesamtkilometer**`-Überschrift). Die früheren `EINTRAG_FLAVORS` (zufällige Motivationszeile, Pendant zu den Ping-Pong-Flavors) wurden dabei **ersatzlos entfernt** – beim Bauen neuer Sport-Antworten also keine Flavor-Zeilen/Überschriften wieder einführen. Ist dieselbe Linie wie die Emoji-Sparsamkeit.
- **Gesamtsumme wird nur in der ANZEIGE gerundet (seit 2026-07-26):** `rundeKilometer(km)` (= `Math.round`, exportiert + getestet) rundet die gemeinsame Gesamtdistanz kaufmännisch auf ganze Kilometer – an den drei Anzeigestellen Eintrags-Embed, `/sport gesamt`, Mitternachts-Post. **Der gespeicherte Wert bleibt exakt** (sonst gingen bei jedem Eintrag Nachkommastellen verloren und die Summe driftete), und die **Meilenstein-Schwellenprüfung** (`checkAndMarkReachedMilestones` im Service) rechnet bewusst weiter mit dem exakten Wert (sonst gälte 1999,5 km schon als 2000er-Schwelle erreicht). `/sport statistik` (persönliche Aufschlüsselung inkl. eigener Summe) bleibt bewusst ungerundet.
- `/sport setzen` (Admin, Kilometerstand eines Users direkt setzen) schreibt nur den Bestenlisten-Score, legt aber keinen `SportEntry` an. Der eigene `/sport statistik`-Wert (aus den individuellen Einträgen berechnet) kann danach vom `/sport gesamt`-Beitrag dieses Users abweichen – als Admin-Korrekturwerkzeug für Altdaten so gewollt, aber gut zu wissen falls das mal für Verwirrung sorgt.
- `LEGACY_KILOMETERS` ist ein Dummy-User in der Bestenliste (`/sport altkilometer`, Admin) für Kilometer ohne zuordenbaren Discord-User – zählt zu `/sport gesamt` dazu, taucht aber nirgends als "Person" auf (seit Entfernung von `bestenliste` sowieso irrelevant, war vorher aber explizit rausgefiltert).
- `/sport altkilometer` **addiert** nur (`incrementSortedSet`) – es gab lange keinen Weg, die Bestandskilometer zu korrigieren oder zu entfernen. Seit 2026-07-04 gibt es dafür `/sport altkilometer-setzen kilometer:<n>` (Admin, `handleAltkilometerSetzen` → `sportService.setLegacyKilometer`), das den Legacy-Wert **direkt setzt** (statt zu addieren) und bei `0` den Legacy-Eintrag per neuem `redisService.removeFromSortedSet` (`zRem`) **ganz aus dem Sorted-Set entfernt**, statt einen 0-Score-Eintrag zu hinterlassen. Die Antwort nennt den vorherigen Wert (`getLegacyKilometer`). Betrifft bewusst **nur** die Bestandskilometer, nicht die individuell eingetragenen User-Kilometer – ein kompletter Gesamt-Reset ist bewusst nicht gebaut (siehe README-Todo).

### Meilensteine (seit 2026-07-07, war README-Todo „Sport-Event bei Schwelle")
- Zweck: Überschreitet die gemeinsame Gesamtdistanz eine Schwelle, postet der Bot einen frei formulierten Ankündigungstext (z.B. „Yay, wir haben 2000 km geschafft!") in einen konfigurierten Kanal. **Nichts mit dem `/event`-Feature zu tun** (das ist der Community-Termin-Countdown).
- Commands: **`/sport meilenstein setzen kilometer:<n> text:<text>` ist bewusst für ALLE offen** (User legen die Meilensteine an – so war der ursprüngliche Wunsch „von Usern angelegt"), `text` mit `\n`→echtem Umbruch + voller Markdown-Formatierung wie bei `/rollenbutton`. `/sport meilenstein liste` + `/sport meilenstein entfernen kilometer:<n>` sind **Admin**. `meilenstein` ist eine **Subcommand-Gruppe** (`getSubcommandGroup(false)` im Dispatch) – der Drift-Test in `sport.command.test.ts` prüft die Top-Level-Optionen (Gruppe zählt als eine). Der **Ankündigungskanal wird seit 2026-07-25 über `/config` gesetzt** (`/sport ankuendigungskanal` entfernt); der `SPORT:ANNOUNCEMENT_CHANNEL`-Key + die Auto-Erfassung/Meilenstein-Logik bleiben unverändert.
- Speicherung: Redis-**Hash** `SPORT:MILESTONES`, Feld = Kilometerzahl (String), Wert = JSON `{kilometers, text, announced}` (neue Redis-Helper `setHashField`/`getHashAll`/`deleteHashField`). Kanal unter `SPORT:ANNOUNCEMENT_CHANNEL`.
- **`announced`-Flag pro Meilenstein** (User-Wunsch „einmal erreicht ist erreicht"): `checkAndMarkReachedMilestones()` nimmt `getGesamtKilometer()` (**alles zählt** – auch Legacy/Admin-gesetzte Werte, Admins müssen aufpassen), markiert alle noch nicht angekündigten, erreichten Meilensteine als `announced` und gibt sie zurück; das eigentliche Posten macht der Handler (braucht `client`).
- Verkabelt in **allen summen-ERHÖHENDEN** Pfaden via `sportHandler.announceReachedMilestones()`. **Nicht** in `handleLoeschen`/`entferneMeilenstein` (Verkleinern kann keine neue Schwelle auslösen; einmal gefeiert bleibt gefeiert). Die Pfade liegen seit der `/config`-Migration an **zwei** Stellen, das ist die Stolperfalle: die User-Aktionen in `sport.handler.ts` (`handleEintragen`/`handleBearbeiten`/Auto-Erfassung, jeweils nach der Reply) und die **Admin-Aktionen in `config.settings.ts`** (`speichereKilometer`/`addiereLegacyKilometer`/`setzeLegacyKilometer`). Deshalb ist `announceReachedMilestones()` seit 2026-07-26 **public** statt privat. **Regression 1.55.0–1.60.0:** beim Verschieben der Admin-Commands auf `/config` fiel der Aufruf in den drei Admin-Pfaden ersatzlos weg – setzte ein Admin Kilometer über eine Schwelle, passierte nichts, und die Feier hing sich später an den nächsten fremden User-Eintrag (nicht verloren, weil ohne Aufruf auch nichts als `announced` markiert wird, aber im falschen Moment). Ein Test in `config.settings.test.ts` nagelt die drei Aufrufe jetzt fest. **Merksatz:** wandert eine summen-ändernde Aktion woandershin, muss der Meilenstein-Aufruf mitwandern.
- `announceReachedMilestones()` ist bewusst **fehlertolerant** (try/catch außenrum): darf eine erfolgreich gespeicherte Aktivität nie nachträglich scheitern lassen. **Ohne konfigurierten/abrufbaren Kanal wird `checkAndMarkReachedMilestones` gar nicht erst aufgerufen** → nichts wird als `announced` markiert → die Feier wird nachgeholt, sobald ein Kanal existiert und die Summe erneut steigt. (Rand-Fall: ist der Kanal da, aber `channel.send` scheitert, gilt der Meilenstein trotzdem als announced – wie bei Twitch bewusst nur geloggt.)
- `sport.handler.ts` importiert seit 2026-07-07 `client` (nur in Methodenkörpern, nie Top-Level → Zirkular-Import-Falle umgangen, wie `twitch.handler.ts`).

## Logging-Feature (Audit-Log, Kanal via `/config`)

- Der Ziel-Channel für Nachrichten-Logs (`LOGGING:CHANNEL`, `logging.service.ts`) wird seit 2026-07-25 über **`/config`** gesetzt. Der frühere flache Command `/protokoll` (Datei `log.command.ts`, `loggingHandler.handleSetChannel`) wurde dabei **ersatzlos entfernt** – der Setter `loggingService.setLogChannel` bleibt (von `config.settings` genutzt). (Historie: Command 2026-07-04 von `/log` auf `/protokoll` eingedeutscht, 2026-07-25 zugunsten von `/config` entfernt.)
- `client.ts` braucht `partials: [Partials.Message, Partials.Channel]`, sonst feuern `MessageDelete`/`MessageUpdate` für nicht (mehr) gecachte Nachrichten gar nicht erst – ohne das würde das Feature den Großteil der Deletes/Edits einfach verpassen.
- **Nachrichten-Cache (seit 2026-07-11):** Der alte Inhalt kam vorher nur aus dem RAM-Cache von discord.js (nach jedem Neustart leer, ältere Nachrichten immer `*nicht verfügbar*`). Jetzt schreibt `handleMessageCreate` (verkabelt in `index.ts` als **eigene** `MessageCreate`-Registrierung neben `blahajHandler`/`sportHandler`) Text + Autor-Tag + **Anhang-Dateinamen** nach `LOGGING:MESSAGE:<messageId>` mit **7 Tagen TTL** (`MESSAGE_CACHE_SECONDS`), Delete/Update lesen daraus, wenn discord.js nichts hat. Drei bewusste Grenzen: **nur wenn ein Log-Channel konfiguriert ist** (ohne `/protokoll` speichert der Bot weiterhin gar keinen Nachrichteninhalt), Eintrag wird **nach dem Lösch-Log sofort entfernt**, und Anhänge werden **nur namentlich** protokolliert (keine Dateien, keine CDN-Links – die sterben mit der Nachricht). Das ist eine bewusst getroffene Datenschutz-Entscheidung, kein Implementierungsdetail – dokumentiert in `docs/datenhaltung.md`, dort auch die vollständige Übersicht aller Redis-Daten. Neue Features, die Daten speichern, gehören in diese Tabelle.
- Die Änderungs-Erkennung bei `MessageUpdate` vergleicht seitdem die **aufgelösten** Inhalte (RAM-Cache *oder* Redis) statt `oldMessage.partial` als „unbekannt" zu behandeln – sonst würde ein Embed-Unfurling auf einer nur in Redis bekannten Nachricht fälschlich als Bearbeitung geloggt.
- Ist eine Nachricht älter als die TTL (oder war das Logging beim Verfassen aus), ist der alte Inhalt weiterhin nicht rekonstruierbar (Discord-API-Grenze, kein Bug) – zeigt dann einen Fallback-Text statt zu crashen.
- Bot-Nachrichten werden ignoriert (sonst loggt der Bot ständig seine eigenen Antworten). `MessageUpdate` ohne echte Content-Änderung wird ebenfalls ignoriert (Discord feuert das Event z.B. auch beim Nachladen von Link-Embeds, nicht nur bei echten Edits).
- **Alle Log-Posts laufen über `sendeLog` → `kuerzeFuerDiscord` (2000-Zeichen-Limit, seit 2026-07-23):** Discord lehnt Nachrichten >2000 Zeichen mit Fehler `50035` ab. Ein Log-Post aus dynamischem Inhalt (v.a. eine gelöschte/bearbeitete **lange** Nachricht + Präfix) kann das reißen – der `try/catch` in jedem Handler fängt den Fehler zwar (kein Crash), aber der Log-Eintrag ginge verloren. Deshalb wird zentral gekürzt statt scheitern gelassen. `kuerzeFuerDiscord` ist exportiert + getestet. (Hintergrund: aufgefallen bei einem `50035` im Prod-Log; die send-Pfade waren schon crash-sicher, der Verlust des Log-Eintrags war das eigentliche Manko.)
- **Beitritts-Zähler (seit 2026-07-11):** Die Beitritts-Meldung nennt bei Wiederkehrern, das wievielte Mal es ist („bereits zum 3. Mal") – Discord selbst merkt sich das nicht. Zähler in Redis (`MEMBER:JOIN_COUNT:{userId}`, dauerhaft, neuer `member.service.ts` + `redisService.increment` = `INCR`). Zwei bewusste Details: (1) Hochgezählt wird **in `handleGuildMemberAdd` selbst**, obwohl `member.handler.ts` auf dasselbe Event hört – `INCR` liefert den neuen Stand in einem Schritt zurück, während „in Handler A zählen, in Handler B lesen" ein Rennen zwischen zwei async-Listenern wäre. (2) Gezählt wird **vor** dem Log-Channel-Check, also auch ohne konfiguriertes Logging – sonst stünde die Zahl später falsch da. Beim ersten Beitritt wird sie **nicht** genannt (dort ist sie keine Information). Bestandsmitglieder starten bei 0: die Zählung beginnt ab jetzt, für alle Altbeitritte gibt es keine Daten.
- Loggt seit 2026-07-03 zusätzlich Server-Beitritt/-Austritt (`GuildMemberAdd`/`GuildMemberRemove`) in denselben Log-Channel. `member.handler.ts` hört auf **dieselben** Events für einen anderen Zweck (aktuell nur `console.log`, historisch war da mal mehr geplant) – zwei separate `client.on(...)`-Registrierungen für dasselbe Event in unterschiedlichen Dateien sind hier bewusst so, nicht redundant: unterschiedliche Zuständigkeiten (User-Daten-Tracking vs. Audit-Log-Channel).
- Loggt seit 2026-07-04 zusätzlich über **`GuildMemberUpdate`** (`handleGuildMemberUpdate`) drei Dinge in einem Durchlauf, die sich sonst überlappen: **Rollenänderungen** (Diff `newMember.roles.cache` vs. `oldMember.roles.cache` → `➕ … erhalten` / `➖ … verloren`), **Nickname-Änderungen** (`oldMember.nickname` vs. `newMember.nickname`, inkl. gesetzt/entfernt) und **Timeouts/Mutes** (`communicationDisabledUntilTimestamp` → `🔇 stummgeschaltet bis <t:…>` / `🔊 aufgehoben`). Alle Meldungen werden in eine `messages`-Liste gesammelt und nur bei nicht-leer der Log-Channel geholt/beschrieben. `GuildMemberUpdate` ist damit – wie GuildMemberAdd/Remove – **doppelt registriert**: `member.handler.ts` (`saveUser`, User-Daten) und `logging.handler.ts` via `index.ts` (Audit-Log), bewusst getrennt. Wichtig: Bei **nicht gecachtem** altem Mitglied (`oldMember.partial`) wird abgebrochen – sonst würde der leere Alt-Zustand jede bestehende Rolle fälschlich als "gerade erhalten" loggen. Kein zusätzliches Partial nötig, da `loadAllMembers()` beim Start alle Mitglieder cached (`GuildMembers`-Intent ist schon an). Rollenvergaben des Bots selbst (Button-Rollen) tauchen hier ganz normal als Änderung auf.
- Loggt seit 2026-07-04 außerdem **Bans/Unbans** (`GuildBanAdd`/`GuildBanRemove` → `handleGuildBanAdd`/`handleGuildBanRemove`, Grund wird mitgeloggt falls vorhanden) und **Massen-Löschungen** (`MessageBulkDelete` → `handleMessageBulkDelete`, postet Anzahl + Quell-Channel). Massen-Löschungen (z.B. Mod-Purge) feuern **nicht** als einzelne `MessageDelete`-Events, wurden vom Nachrichten-Logging vorher also komplett verpasst. Bans brauchen den zusätzlichen `GatewayIntentBits.GuildModeration`-Intent (`client.ts`); `MessageBulkDelete` läuft über den schon vorhandenen `GuildMessages`-Intent.

## Rollen-Selbstvergabe / Button-Rollen (`/rollenbutton`)

- Zweck: User geben sich **selbst** Rollen (z.B. "Einwohner" als Regel-Akzeptanz beim Server-Beitritt, oder die Twitch-Benachrichtigungs-Rolle zum Selbst-An/Abmelden). Am 2026-07-04 von einer Reaction-basierten Variante (`/rolle`, User reagiert mit Emoji) auf **Buttons** umgestellt – Buttons sind der modernere, sauberere discord.js-v14-Weg: kein Zumüllen der Nachricht mit Fremd-Reactions, klares ephemeres Feedback, selbsterklärendes Toggle. Die alte Reaction-Variante (`reactionRole.handler.ts`/`.service.ts`/`rolle.command.ts` + `REACTIONROLE:`-Redis-Keys) wurde dabei komplett entfernt, nicht parallel behalten.
- `/rollenbutton text:<text> rolle:<@Rolle> beschriftung:<beschriftung> [emoji:<emoji>]` (Admin) lässt den **Bot eine neue Nachricht posten** mit dem Text und einem Button darunter. Bot postet via `interaction.channel.send(...)`, quittiert dem Admin nur ephemer – die Button-Nachricht selbst ist also eine Bot-Nachricht (nötig, weil Discord Buttons nur an Bot-eigene Nachrichten erlaubt).
- Der `text` ist eine normale Bot-Nachricht → **volle Discord-Markdown-Formatierung** (fett, Überschriften, Zitate, …). Seit 2026-07-05 wandelt `handleCreate` literal getipptes `\n` per `.replaceAll('\\n', '\n')` in echte Zeilenumbrüche um – Slash-Eingabefelder erlauben kein echtes Enter, ohne das gäbe es keine mehrzeiligen Button-Nachrichten. Die **Button-`beschriftung`** ist dagegen **nicht** formatierbar (Discord-Grenze: Button-Labels sind reiner Text + optional ein `emoji`), kein Code kann das ändern.
- **Kein Redis, kein State:** Die Rolle steckt direkt in der Button-`customId` (`role-toggle:{roleId}`). Beim Klick liest `buttonRoleHandler.handleButton` die Rolle da raus und **toggelt** sie (User hat sie → entfernen, sonst → vergeben), ephemere Rückmeldung jeweils. Dadurch überlebt die Bindung jeden Bot-Neustart automatisch, ohne dass irgendwo etwas gespeichert werden muss. `reactionRole.service.ts` ist damit ersatzlos weggefallen.
- Verkabelung: Buttons sind **Interactions**, keine Reactions. `interaction.handler.ts` routet `interaction.isButton()` an `buttonRoleHandler.handleButton`. Dadurch fielen `GatewayIntentBits.GuildMessageReactions` + `Partials.Reaction`/`Partials.User` (`client.ts`) und die beiden `MessageReactionAdd`/`-Remove`-`client.on`-Zeilen in `index.ts` wieder weg – Buttons brauchen davon nichts.
- Setzt voraus, dass der Bot "Rollen verwalten"-Rechte hat **und** seine eigene Rolle in der Server-Hierarchie über der zu vergebenden Rolle steht – sonst kann `member.roles.add/remove` die Rolle nicht setzen. Bleibt reine Server-Konfiguration (kein Code kann fehlende Rechte erzeugen), **aber** seit 2026-07-04 prüfen sowohl `handleCreate` (beim Anlegen) als auch `handleButton` (beim Klick) das **proaktiv** (`me.permissions.has(ManageRoles)` + `me.roles.highest.comparePositionTo(role) <= 0`) und geben eine klare ephemere Meldung ("Rollen verwalten fehlt" / "Rolle steht über meiner in der Hierarchie") statt erst beim eigentlichen API-Call an einem generischen `Missing Permissions` zu scheitern. Der `handleButton`-catch bleibt als letztes Netz für alles andere.
- `buttonRole.handler.ts` importiert `client` **nicht** – die zirkuläre-Import-Falle (siehe Stolperfallen) fällt hier von vornherein weg. Die `client.on(...)`-Registrierung lebt ohnehin in `interaction.handler.ts` (das `client` importiert, aber *nicht* über `commands/index.js` erreichbar ist).

## Member-Handling (`src/handlers/member.handler.ts`)

- Einzige Handler-Datei, die (bis 2026-07-03) Logik direkt inline in `client.on(...)`-Callbacks statt als benannte Klassenmethoden hatte – deshalb ungetestet und beim Command-Block-Review leicht übersehen (kein Slash-Command). Jetzt wie die anderen Handler als `MemberHandler`-Klasse mit Methoden, `memberHandler` als Default-Export, `client.on(...)`-Verkabelung bleibt in der Datei selbst (nicht in `index.ts`, nur `loadAllMembers()` wird von dort aufgerufen).
- `handleUserUpdate` nutzt `client.guilds.cache.get(config.GUILD_ID)` statt `.first()` – der Bot ist aktuell ohnehin fest auf eine Guild ausgelegt (siehe README-Todo "Bot generalisieren für jeden Server"), aber `.first()` war eine implizite Annahme statt einer expliziten Referenz auf die konfigurierte Guild.

## Event-Feature (`/event`)

- Zweck (seit 2026-07-04, war README-Todo „Tage bis zum Treffen"): Ein Admin setzt einen Termin für das Community-Event, alle User können per Countdown „wie lange noch?" fragen. **Setzen/Entfernen laufen seit 2026-07-25 über `/config`** (native Datums-/Zeit-Felder → `parseIsoDateTime` in `config.router.ts`); die früheren `/event setzen`/`entfernen` sind **entfernt**. Als Commands bleiben `/event countdown` (alle) + `/event hilfe`. `parseGermanDateTime` (event.handler.ts) wird von keinem Command mehr aufgerufen, bleibt aber als exportierte+getestete Utility stehen (das Web-Formular nutzt `parseIsoDateTime`, weil die nativen Felder ISO liefern).
- **Kein Datum-Objekt in Redis**, nur ein Unix-Timestamp (ms) + Titel als JSON unter `EVENT:NEXT` (`event.service.ts`). Genau **ein** Event gleichzeitig (Setzen überschreibt).
- Datum-Parsing (`parseGermanDateTime`, exportiert + separat getestet): deutsches Format `TT.MM.JJJJ` + optional `HH:MM`, gebaut mit `new Date(year, month-1, day, …)` (lokale TZ des Hosts = Europe/Berlin). **Wichtig:** danach wird per Round-Trip-Vergleich (`date.getDate() === day` etc.) validiert, weil `new Date()` ungültige Werte wie `32.13.` still auf den nächsten gültigen Tag normalisiert – ohne den Check würde Quatsch klaglos akzeptiert. Vergangene Daten werden beim Setzen abgelehnt.
- Anzeige nutzt **Discord-Timestamps** (`<t:unix:F>` absolut, `<t:unix:R>` relativ) – die lokalisieren sich pro Betrachter automatisch. Zusätzlich eine selbst ausgerechnete deutsche Restdauer (`formatRemaining`, exportiert + getestet): „X Tage und Y Stunden", Minuten nur wenn <1 Tag, mit korrektem Singular/Plural.
- Der `titel` ist **optional** und wird nur gespeichert, wenn angegeben (kein Default-String mehr). Der Countdown-Satz ist bewusst „**Noch {dauer}** bis zum nächsten Event{ – **titel**}!" formuliert: der Titel steht nach einem Gedankenstrich, **nicht** in einer Präposition – so entfallen Artikel-/Genus-Probleme (`bis zum {titel}` würde bei „die LAN-Party" o.ä. falsch), und die „Noch X"-Konstruktion umgeht das ist/sind-Agreement bei Singular vs. Plural der Restdauer.
- Ist **kein** Event gesetzt, antwortet `/event countdown` seit 2026-07-05 nicht mehr sachlich, sondern mit einer **zufälligen spielerischen Variante** von „Noch viel zu lange" (`randomNoEventReply()` aus `NO_EVENT_REPLIES`, beide exportiert + getestet) – bewusst im selben „Noch X"-Duktus wie der echte Countdown.
- `event.handler.ts` importiert `client` **nicht** (arbeitet nur mit `interaction`), Verkabelung läuft über `interaction.handler.ts` wie bei allen Slash-Commands – zirkuläre-Import-Falle also kein Thema.

## News-Feature (`/news` + `/ereignisse`)

- Zweck (seit 2026-07-04, war README-Todo): `/news` (alle) holt die neueste Spiel-News von `https://www.lotgd.de/news.php` **live per Scraping** und postet sie als einfachen Text (Titel, Datum, gekürzter Fließtext, Link zum Weiterlesen). Bewusst nur auf Abruf, kein automatisches Posten/Polling und kein State (die einfachste Variante war explizit gewünscht).
- **Es gibt keinen Feed/keine API** – nur HTML, und das ist **ISO-8859-1** kodiert. Der Abruf läuft über den geteilten `fetchLotgdHtml`-Helper (`lotgd.service.ts`, siehe Konventionen), der explizit mit `new TextDecoder('iso-8859-1')` dekodiert – sonst kommen kaputte Umlaute raus.
- Parsing ohne zusätzliche Dependency (`parseLatestNews`/`htmlToText`, exportiert + getestet): Jeder News-Eintrag ist als `<a name='motdJJJJMMTTHHMMSS'> … <hr></p>` eingefasst. Der `motd`-Anker liefert das Datum direkt (kein Locale-Parsing), das `</p>` ist die Ende-Grenze. Titel = erstes `<b>…</b>`, Body = alles hinter dem `colLtCyan`-Datums-Span bis zum `<hr>`. `<br>`→Zeilenumbruch, restliche Tags (v.a. LotGDs `<span class='colXXX'>`-Farbsoup) raus, gängige HTML-Entities dekodiert.
- **Fragil by design** (Scraping): ändert lotgd.de sein HTML/seine CSS-Klassen, liefert `parseLatestNews` `null` → Handler meldet „konnte nicht abrufen", crasht aber nicht. `getLatestNews` fängt zusätzlich Netzwerk-/Parse-Fehler ab und gibt `null` zurück. `handleNews` nutzt `deferReply()` (Netz-Call) und kürzt den Text auf 1500 Zeichen (Discord-2000-Limit).
- `news.handler.ts`/`news.service.ts` importieren `client` nicht – zirkuläre-Import-Falle kein Thema.
- **`/ereignisse` (seit 2026-07-09, war README-Todo „Neuigkeiten am"-Teil):** `news.php` hat **zwei** Abschnitte – oben die MOTD-Ankündigungen (`/news`), darunter unter der Überschrift `Neuigkeiten am <Datum>` das **Ingame-Ereignislog** (Spielgeschehen: wer wurde von wem getötet/wiederbelebt/blamiert). Beides holt `news.service.ts` aus **derselben** Seite (je Aufruf ein `fetchLotgdHtml`).
- Bewusst ein **eigener flacher Command** statt `/news`-Subcommand: sobald `/news` einen Subcommand bekäme, verlangt Discord *immer* einen, das blanke `/news` würde wegfallen (siehe Hilfe-Feature). Als flacher Command wird `/ereignisse` deshalb nur in `/hilfe` dokumentiert – `hilfe.handler.test.ts` sichert das per `it.each` ab.
- `parseGameEvents` (exportiert + getestet) schneidet **hinter dem `</div>` der Überschrift** an, nicht direkt hinter dem Datum: in derselben Überschrift steht noch der Paginierungs-Rest (`(Nachrichten 1 - 50 of 628)`), der sonst als erstes „Ereignis" im Chat landet. Ende-Grenze ist `<div id="petition">` (die Seitenleiste). Einträge sind durch `<div align='center'>…-=-=-…</div>`-Trenner separiert – einer davon trägt auf der echten Seite ein verirrtes `</span>` direkt hinter dem `<div>`, deshalb matcht der Trenner-Regex großzügig bis zum ersten `</div>` nach dem `-=-`.
- Das Überschriften-Datum ist **englisch** (`Thu, Jul 9, 2026`) und wird über eine Monats-Map ins deutsche `TT.MM.JJJJ` gebracht; unbekannte Formate bleiben unverändert stehen, statt zu scheitern.
- Die Seite liefert 50 Ereignisse, `/ereignisse` zeigt die **neuesten 5** (`EVENT_COUNT`). Beim 2000-Zeichen-Limit werden **ganze Ereignisse weggelassen** statt Sätze abgeschnitten (nur wenn schon das erste zu lang ist, wird es gekürzt) – Ereignisse sind vollständige Sätze, halbe wären unlesbar.

## Online-Feature (`/online`)

- Zweck (seit 2026-07-10, war README-Todo/Spiel-Anbindung): `/online` (alle) zeigt live per Scraping, wer gerade im Spiel eingeloggt ist. Quelle ist `https://www.lotgd.de/list.php` (die „Kriegerliste") – **öffentlich ohne Login abrufbar** (im Gegensatz zu Charakterdaten/Mail, die ein programmatisches Login bräuchten). Eigener Service `online.service.ts`, flacher Command wie `/news`/`/ereignisse` (nur in `/hilfe` dokumentiert, per `it.each` abgesichert).
- Wie News: **ISO-8859-1**, Bot-`User-Agent` (der Standard-Fetch-UA bekommt `403`), fehlertolerant. `getOnline()` macht **einen** Netz-Call und liefert daraus beide Listen.
- **Zwei Listen aus einer Seite** (Wunsch „beide kombiniert"): (1) `parseOnlinePlayers` = die reiche Haupttabelle „gerade eingeloggt" mit Spalten Gilde/Name/Ort/Level/Rasse/Geschlecht/Lebt (Geschlecht wird aktuell nicht angezeigt); (2) `parseRecentlyOnline` = die Seitenleiste „Online letzte 30 Minuten" (nur Namen). Der Handler hängt die 30-Min-Namen an, **dedupliziert** gegen die schon Eingeloggten.
- `parseOnlinePlayers` gibt **`[]`** zurück (niemand eingeloggt, Struktur ok) vs. **`null`** (Anker/Tabelle weg = Markup geändert) – der Handler unterscheidet: „niemand eingeloggt" vs. „konnte nicht abrufen". Tote Charaktere (`Lebt` = Nein) bleiben in der Liste, nur mit `(tot)` markiert.
- **Nach Stadt gruppiert (seit 2026-07-21, war Tour-Todo):** Die eingeloggten Spieler stehen unter einer Stadt-Überschrift (`__Romar__ (2)`), der Ort ist deshalb **aus der einzelnen Spielerzeile raus** (steht nur noch als Gruppen-Header). `groupByCity` (in `online.handler.ts`, exportiert + getestet) sortiert die Gruppen **nach Größe absteigend** (belebte Städte zuerst), bei Gleichstand alphabetisch (`localeCompare('de')`) – stabile Ausgabe. Nutzt die ohnehin geparste Ort-Spalte, **kein zusätzlicher Fetch**. Das 2000-Zeichen-Budget lässt bei Überlauf ganze Zeilen weg (Flag `voll`), Stadt-Header werden mitgezählt. Namen sind auf der Seite oft Buchstabe-je-Farb-`<span>` zerlegt (Regenbogennamen) – `htmlToText` (aus `news.service.ts`, wiederverwendet) fügt sie zusammen.
- **Nebenfix am geteilten `decodeEntities` (news.service):** um die vollen Latin-1-Akzentbuchstaben (`&Uacute;`→Ú etc.) + hex-numerische Entities (`&#xDA;`) erweitert, weil Spielernamen sonst als `&Uacute;tlaga` durchrutschen. Kam `/news`/`/ereignisse` mit zugute.

## Charakter-Feature (`/charakter`)

- Zweck (seit 2026-07-10, war README-Todo „mit dem Spiel interagieren", lesender Teil): User verknüpfen ihren LotGD-Charakter und rufen dessen öffentliche Infos ab. **Bewusst „Weg A": nur der öffentliche Charaktername wird gespeichert, KEINE Zugangsdaten.** Grund: lotgd.de hasht das Passwort client-seitig per md5 und akzeptiert diesen Hash am `login.php` → der **Hash ist login-äquivalent**, „nur den Hash speichern" bringt keinen Sicherheitsgewinn. Für einen Hobby-Bot ist das gehortete Login-Geheimnisse Dritter = unverhältnismäßige Haftung. Details/Stufen 2+3 (private Daten, Aktionen – zurückgestellt): `docs/spiel-interaktion-idee.md`.
- Gruppen-Command (darf daher ein `hilfe`-Subcommand haben, konsistent mit `/sport`/`/twitch`): `verknuepfen name:` / `anzeigen [name:]` / `entfernen` / `hilfe`. `/hilfe` verweist im „Spielwelt"-Block auf `/charakter hilfe`. `/spielwelt` bleibt die Erklärung der drei **flachen** Info-Befehle (`/news`/`/ereignisse`/`/online`); `/charakter` ist eigenständig.
- Datenquelle: das **paginierte** öffentliche Roster `list.php?op=bypage&page=N` (~695 Charaktere, 100/Seite, auch offline). 8 Spalten inkl. **„Zuletzt da"**. `character.service.ts`: ISO-8859-1 + Bot-UA (wie news/online), `parseRoster` (`htmlToText` für Regenbogen-Namen, `null` bei kaputtem Markup), `getRoster` blättert bis zur ersten nicht-vollen Seite und **cacht in-memory** (`ROSTER_CACHE_MS` = 10 min; `clearCache()` erzwingt Neuabruf), sonst 7 Fetches pro Aufruf.
- **Titel-Präfix-Falle:** Der Roster zeigt „<Titel> <Name>" (z.B. „Centurio Acaine"), der Titel ändert sich mit Level/Drachentötungen. Gespeichert wird der eingegebene **Kern-Name**, `findInRoster` matcht **Suffix auf Wortgrenze** (exakter Name ODER endet auf „ Name") – sonst bräche die Zuordnung beim nächsten Titelwechsel. Randfall: zwei Charaktere mit gleichem Suffix → der erste gewinnt (bewusst simpel gehalten).
- Handler unterscheidet **„konnte nicht abrufen"** (`getRoster()` → `null`, Abruf/Markup kaputt) von **„nicht gefunden"** (`findInRoster` → `null`), deshalb ruft er beide getrennt auf statt einer Bequem-Methode. Karte ist ein **Embed** (Titel = aktueller voller Name, Farbe grün/dunkelrot nach lebendig/tot), analog zur `/sport eintragen`-Embed-Entscheidung. **Toten-Flavor (seit 2026-07-21, war Tour-Todo):** tote Charaktere bekommen statt nur „tot" eine zufällige lore-stimmige Zeile aus `TOTEN_FLAVORS` (`character.handler.ts`, exportiert + getestet; `randomTotenFlavor()`), die die LotGD-Wiederauferstehung aufgreift (Anbruch des neuen Tages / Gefallen beim Totengott Ramius). `buildCard` ist dadurch nicht mehr deterministisch – der `buildCard`-Test prüft weiter nur Titel, der Flavor wird über die Konstante/Funktion separat getestet. Beim `verknuepfen` wird der Name **gegen das Roster validiert** (Tippfehler abfangen); ein bestehender Link blockt (wie `/twitch`).
- `character.service.ts` hält zusätzlich `getAllLinks()` (Name ↔ Discord-User) als Basis für den Abgleich in `/online`/`/ereignisse` (siehe nächster Punkt). `character.handler.ts` importiert `client` nicht.

### Hervorhebung verknüpfter Charaktere in `/online` + `/ereignisse` (seit 2026-07-11)
- Taucht ein verknüpfter Charakter in der Online-Liste bzw. im Ereignislog auf, wird sein Name **fett** gesetzt und der Discord-User dahinter genannt (`<@id>`). Die Antworten gehen deshalb als **Objekt** raus (`editReply({content, allowedMentions: {parse: []}})`, vorher reiner String) – **`allowedMentions` ist Pflicht**, sonst pingt jedes `/online` alle verknüpften User an. Tests lesen entsprechend `editReply.mock.calls[0][0].content` (die reinen Fehler-/Leer-Antworten bleiben Strings).
- Zwei Matcher in `character.service.ts` (exportiert + getestet): **`findLinkForName`** für Anzeigenamen aus dem Spiel (Suffix-Match auf Wortgrenze wie `findInRoster`, wegen des Titel-Präfixes) und **`findLinksInText`** für den Ereignis-Fließtext. Letzterer benutzt Unicode-Lookarounds (`(?<![\p{L}\p{N}])…`) statt `\b` – `\b` kennt „Ú" nicht als Wortzeichen, „Útlaga" würde am Satzanfang nie matchen.
- **Fehlertolerant:** `getAllLinks()` läuft in beiden Handlern in einem eigenen try/catch (`links = []` im Fehlerfall) – ein Redis-Problem darf `/online`/`/ereignisse` nicht kosten, die Liste kommt dann nur ohne Hervorhebung.

## Hilfe-Feature (`/hilfe` + `hilfe`-Subcommands)

- Zweck (seit 2026-07-05, war README-Todo): Nutzer sollen ohne Vorwissen die Befehle finden. Zwei Ebenen: **Gruppen-Befehle** (`/sport`, `/twitch`, `/event`) haben je ein `hilfe`-Subcommand mit ihren eigenen Befehlen; **`/hilfe`** ist die neue **Gesamtübersicht** über alle Bereiche (flache Command-Datei `hilfe.command.ts` → `hilfe.handler.ts`, wie `/news`/`/version`).
- **Bewusste Design-Entscheidung:** flache Einzelbefehle (`/rollenbutton`, `/news`, `/version`, `/blahaj`, …) bekommen **kein** eigenes `hilfe` – sobald ein Command *einen* Subcommand hat, verlangt Discord *immer* einen (das blanke `/news` würde wegfallen). Sie werden deshalb **nur** in `/hilfe` erklärt.
- **Die Hilfe-Absicherung in `hilfe.handler.test.ts` wird seit 2026-07-26 aus `commands/index.ts` ABGELEITET, nicht gepflegt** (vorher eine hartcodierte `it.each`-Liste): „flach" = keine Option vom Typ 1/2 (SUB_COMMAND/SUB_COMMAND_GROUP), und jeder flache Befehl außer der Ausnahmeliste `NUR_ADMIN` (`diagnose`, `rollenbutton`) muss im `HELP_TEXT` stehen. Grund: eine handgepflegte Liste bleibt grün, wenn jemand einen neuen flachen Befehl hinzufügt und die Hilfe vergisst – also genau im Fall, den der Test abfangen soll. Ein neuer Befehl erzwingt jetzt einen `HELP_TEXT`-Eintrag **oder** eine bewusste Ausnahme. Dazu drei Gegenproben: die Admin-Befehle dürfen **nicht** drinstehen, jede Gruppe muss per `/<name> hilfe` verlinkt sein, und ein Sanity-Test stellt sicher, dass die Ableitung überhaupt Befehle findet (`it.each([])` liefe sonst als „grün" durch). **Der Test muss `client.js` mocken** – `commands/index.js` zieht alle Handler hoch und läuft sonst in die Zirkular-Import-Falle (`default is not iterable`).
- **Jede Gruppen-Hilfe muss alle eigenen Subcommands nennen** (Test seit 2026-07-26, ebenfalls abgeleitet): die Gruppen-Hilfe ist die *einzige* Dokumentation der Subcommands (die Übersicht nennt nur die wichtigsten), ein dort fehlender Subcommand ist schlicht unauffindbar. Der Test ruft das echte `execute()` mit `getSubcommand() === 'hilfe'` auf und prüft den Antworttext – braucht also keine Zuordnung Command→Handler. Aufgefallen an `/charakter hilfe`, das sich selbst nicht auflistete.
- `HELP_TEXT` ist ein exportierter Konstanten-String (getestet: <2000 Zeichen fürs Discord-Limit, verweist für Gruppen auf `/sport|twitch|event hilfe`). Antworten sind **öffentlich** (konsistent mit den bestehenden `/sport hilfe`/`/twitch hilfe`), nicht ephemer.
- **`/spielwelt` (seit 2026-07-10)** ist die Detail-Hilfe für die drei Spiel-Daten-Befehle `/news`/`/ereignisse`/`/online`. Weil die flach sind und **kein** eigenes `hilfe`-Subcommand haben können (siehe Design-Entscheidung), bündelt dieser **eigene flache Befehl** ihre Erklärung – Parallele zu `/hilfe` (`spielwelt.command.ts` → `spielwelt.handler.ts`, exportierter `SPIELWELT_HELP`, öffentlich). `/hilfe` verweist im „Spielwelt"-Block auf `/spielwelt` (wie bei den Gruppen auf deren `hilfe`). `/spielwelt` ist selbst ein flacher Befehl → steht ebenfalls im `HELP_TEXT` und in dessen `it.each`-Absicherung.
- `hilfe.handler.ts`/`spielwelt.handler.ts` importieren `client` nicht – zirkuläre-Import-Falle kein Thema.

## Tipps & Nettigkeiten (`tipp.service.ts`, seit 2026-07-11)

- Zweck (war README-Todo „Tipps wie im Ladebildschirm"): Leute sollen Befehle entdecken, ohne `/hilfe` zu kennen. Dazu manchmal eine Zeile aus `TIPPS` – und, auf User-Wunsch, mit ~30 % Chance stattdessen eine aus `NETTIGKEITEN` (kleine Grüße ohne Belehrung, „Ich hoffe, du hast einen guten Tag").
- **Die Listen liegen als reine Daten in `src/data/tipps.ts` + `src/data/nettigkeiten.ts`** (17 bzw. 133 Zeilen), der Service re-exportiert sie. Das einzige Verzeichnis dieser Art im Projekt – gerechtfertigt allein durch die Länge: inline hätten die 100 Nettigkeiten die Auswahl-/Drossel-Logik im Service begraben. Bei kurzen Flavor-Listen (Ping-Pong, Sport, Event) bleibt es bewusst beim bisherigen Muster (inline im Handler, exportiert + getestet) – nicht vorschnell alles nach `src/data/` ziehen. Ein Test sichert die Listen gegen **Duplikate** ab (fällt beim Pflegen langer Listen sonst leicht durch, und eine doppelte Nettigkeit merkt man als Nutzer sofort).
- Ton der Nettigkeiten (bewusst): keine Aufgaben, keine Ratschläge im Befehlston, keine Leistungsbewertung – wer die Zeile liest, hat gerade einen Befehl benutzt und soll sich nicht ertappt fühlen. Emojifrei wie alles andere.
- **Die zentrale Design-Entscheidung ist, wo die Zeile landet:** als **ephemeres `followUp`** auf eine ohnehin ausgelöste Slash-Command-Antwort – **nicht** als eigener Post im Channel. Die Ladebildschirm-Analogie trägt nämlich nur halb: ein Ladebildschirm ist tote Zeit, ein Discord-Channel ist es nicht, dort kostet jeder Bot-Post die Aufmerksamkeit **aller** Anwesenden. Ephemer sieht die Zeile nur die Person, die gerade sowieso mit dem Bot interagiert – Clutter für den Rest ist damit strukturell ausgeschlossen (passt zur Emoji-/Noise-Linie des Projekts).
- Verkabelt **zentral** in `interaction.handler.ts` (`zeigeGelegentlichEinenTipp`, nach `command.execute()`), nicht in den einzelnen Handlern – sonst müsste man jeden Handler anfassen und bei jedem neuen Command dran denken. Bewusst **fehlertolerant** (try/catch): ein Tipp darf einen erfolgreich ausgeführten Befehl nie nachträglich scheitern lassen. Seit 2026-07-19 ist die Dispatch-Logik als `handleInteractionCreate` + `zeigeGelegentlichEinenTipp` **exportiert** (Registrierung in `client.on` nur noch als dünner Wrapper mit `.catch()`), damit `interaction.handler.test.ts` den zentralen Verteiler testen kann – vorher war er die einzige ungetestete Logik-Datei.
- Vier Drosseln, alle bewusst: `CHANCE = 0.15` (nicht bei jeder Antwort), Redis-**Tages-Cooldown** pro User (`TIPP:COOLDOWN:{userId}`, TTL 24 h – niemand liest denselben Hinweis dreimal am Abend), **keine Zeile an ephemeren Antworten** (das sind genau die Fehlermeldungen, Cooldown-Abfuhren und Admin-Quittungen – da löst jemand ein Problem und will kein Tutorial) und **keine bei `/hilfe`/`/spielwelt`** (dort ist sie redundant). Die Kontext-Regeln stecken in `kommtTippInFrage(commandName, warEphemer)` – bewusst rein/testbar, ohne Zufall und Redis.
- **Tipps nur zu nie benutzten Befehlen (seit 2026-07-12, war README-Todo):** `TIPPS` ist eine Liste von `{befehl, text}`-Objekten (Top-Level-Command-Name), `interaction.handler.ts` merkt sich **jede** Command-Ausführung per `tippService.merkeBenutztenBefehl` in einem Redis-Set (`TIPP:USED_COMMANDS:{userId}`, dauerhaft, nur Befehlsnamen – dokumentiert in `docs/datenhaltung.md`). Gemerkt wird **vor** allen Abbruchbedingungen (auch ephemere Antworten und `/hilfe` zählen als Benutzung). `randomTipp(benutzteBefehle)` filtert entsprechend; kennt die Person schon alle Tipp-Befehle, kommt stattdessen eine Nettigkeit (`?? randomNettigkeit()`), nie `null`-Stille nach gewonnenem Würfelwurf. Ein Test sichert ab, dass jeder `befehl` auch als `/name` im eigenen `text` vorkommt (sonst filtert der Abgleich an der falschen Stelle). Bestandsnutzer starten mit leerem Set – wie beim Beitritts-Zähler beginnt die Zählung ab Inbetriebnahme.

## Ping-Pong-Feature (`/pingpong`)

- **Ping-Pong ist seit 2026-07-11 reines PvP.** Das alte Solo-Spiel gegen den Bot (`/pingpong` als 50/50-Münzwurf, `handlePingPong` + `WIN_FLAVORS`/`LOSS_FLAVORS`) wurde **ersatzlos entfernt**, ebenso `/pingbestenliste` (samt `pingHighscore.command.ts`). Das war eine bewusste Entscheidung gegen die dokumentierte alte Design-Schwäche (rein additive Scores → die Bestenliste belohnte viel Klicken statt Spielen): Punkte gibt es jetzt **nur** noch aus Duellen gegen echte Personen, und ein Duell kostet den Verlierer auch etwas. Bestandsscores in Redis bleiben unangetastet gültig.
- Übrig sind drei Subcommands unter dem **Gruppen-Command** `/pingpong`: `herausfordern` / `bestenliste` / `hilfe` (Command-Datei heißt weiterhin `ping.command.ts`). Dass `/pingpong` eine Gruppe sein *muss*, ergibt sich aus der Discord-Regel: sobald ein Command *einen* Subcommand hat, verlangt Discord *immer* einen (siehe Hilfe-Feature) – ein blankes `/pingpong` konnte es neben `herausfordern` also gar nicht geben.
- `/pingpong bestenliste` zeigt die Top 10 nach Gesamtpunkten. Score-Key ist aus Legacy-Gründen `userId + REDIS_KEYS.PING_PONG` (Format bewusst nicht geändert, sonst verwaisen alle bestehenden Scores).
- **Anti-Spam-Cooldown** (seit 2026-07-05) – pro User ein Redis-Key mit TTL (`PING_PONG:COOLDOWN:{userId}`, `COOLDOWN_SECONDS = 30`, leicht anpassbar). Er hängt jetzt an `handleHerausfordern` (`redisService.getTimeToLive(...)`, ist noch Zeit übrig → ephemere „warte noch Xs"-Meldung, es wird **nicht** herausgefordert) und verhindert, dass jemand den halben Server in Serie herausfordert. Der **Herausgeforderte** bekommt keinen Cooldown – er hat das Duell nicht initiiert. Redis-Helper dafür: `setWithExpiry` (`SET ... EX`) und `getTimeToLive` (`TTL`, liefert -2/-1 = kein Cooldown aktiv).

### PvP-Duell (`/pingpong herausfordern gegner:@user`, seit 2026-07-11, war README-Todo)
- Ablauf: Der Bot postet eine **öffentliche** Nachricht mit zwei Buttons (Annehmen/Ablehnen). **Nur der Herausgeforderte darf klicken** (`interaction.user.id !== gegnerId` → ephemere Abfuhr). Bei Annahme wird das Match simuliert und die Nachricht per `interaction.update()` durch das Ergebnis ersetzt, **Buttons weg** – das ist zugleich der Schutz gegen mehrfaches Annehmen.
- **Kein Redis-State**: Herausforderer- und Gegner-ID stecken direkt in der `customId` (`pingpong-duell:{annehmen|ablehnen}:{herausfordererId}:{gegnerId}`), exakt wie bei den Button-Rollen → die Herausforderung überlebt einen Bot-Neustart, ohne dass etwas gespeichert werden muss. Nebeneffekt: Herausforderungen laufen nie ab (Discord-Buttons tun das nicht).
- Match: `spieleDuell()` (exportiert + getestet) spielt Ballwechsel à 50/50, bis einer `POINTS_TO_WIN = 3` hat – der Ausgang ist also reiner Zufall, nur mit mehr Dramatik als ein einzelner Münzwurf. Sieger **+1** (`DUELL_WIN`), Verlierer **−1** (`DUELL_LOSS`), aber `Math.max(0, …)` – **niemand kann ins Minus gespielt werden** (bewusst, Community-Bestenliste soll nicht bestrafen). Abschluss-Zeile aus `DUELL_FLAVORS` (exportiert + getestet).
- Selbst-Herausforderung und Bots werden ephemer abgelehnt (ein Bot hätte keinen sinnvollen Score).
- **Siegesserie (seit 2026-07-12, war README-Todo):** `verarbeiteSerie(siegerId, verliererId)` zählt die laufende Serie des Siegers per `redisService.increment` hoch (`PING_PONG:SERIE:{userId}`, INCR legt den Key selbst an und liefert den neuen Stand in einem Schritt) und **löscht** die des Verlierers. Der persönliche Rekord liegt bewusst in einem **eigenen** Key (`PING_PONG:REKORD:{userId}`), damit er die abgerissene Serie überdauert. Erzählt wird erst **ab `MIN_SERIE = 2`** – ein einzelner Sieg ist keine Serie, und ein „neuer Rekord" bei jedem ersten Sieg wäre Lärm (gespeichert wird die 1 trotzdem). Der Textbau liegt in der reinen Funktion `formatSerie(SerienStand)` (exportiert + getestet, `null` = nichts zu erzählen), das Duell-Ergebnis hängt sie nur an; `/pingpong bestenliste` zeigt die laufende Serie ab 2 hinter dem Punktestand.
- Verkabelung: Buttons sind Interactions – `interaction.handler.ts` ruft **beide** Button-Handler nacheinander auf (`buttonRoleHandler.handleButton` + `pingPongHandler.handleDuellButton`), jeder prüft seinen `customId`-Prefix selbst und steigt sonst sofort aus. `pingPong.handler.ts` importiert `client` **nicht** – zirkuläre-Import-Falle also kein Thema, obwohl der Handler über `commands/index.js` erreichbar ist.

### Ansage-Duell (`/pingpong ansageduell`, seit 2026-07-12, war README-Todo)
- **Bewusst ein eigener Subcommand neben `herausfordern`**, keine Option am bestehenden Duell (User-Wunsch): das normale Duell bleibt genau wie es war, die Variante steht daneben. Der **Befehl selbst ist die Ansage** („ich gewinne") – es gibt nichts zu wählen. Gespielt wird **unverändert per `spieleDuell()`** (reiner Zufall, die Ansage beeinflusst den Ausgang nicht).
- **Balancing (am 2026-07-12 direkt nach dem ersten Deploy nachgeschärft – 1.32.0 kannte nur den Bonus und beide Ansage-Richtungen):** Sieg/Niederlage wie immer (+1/−1), dazu **`ANSAGE_BONUS` (+1) bei erfüllter, `ANSAGE_MALUS` (−1) bei blamierter Ansage** – beides trifft nur den **Herausforderer**, denn nur er hat angesagt. Bonus und Malus sind **absichtlich gleich groß** (Erwartungswert null): eine reine Belohnung machte das Ansage-Duell strikt besser als das normale (im Schnitt +0,5 gratis) und hätte `herausfordern` verdrängt. Aus demselben Grund gibt es **keine Ansage „ich verliere"** – mit Bonus *und* Malus käme sie rechnerisch immer auf 0 heraus (−1+1 bzw. +1−1), also eine tote, risikofreie Option. Die **Siegesserie richtet sich weiter nach dem echten Spielausgang**, nicht nach der Ansage.
- Zustandslos wie das normale Duell: die Duell-Art steckt **im Prefix** der `customId` (`pingpong-ansage:{annehmen|ablehnen}:{herausfordererId}:{gegnerId}`, ansonsten identisch zum normalen Duell). `handleDuellButton` ist der Einstiegspunkt für **beide** Arten (`istAnsageDuell = prefix === ANSAGE_PREFIX`) – dadurch bleibt die Match-Auflösung an einer Stelle. `formatAnsage(istAnsageDuell, …)` (exportiert + getestet) baut die Ergebnis-Zeile, `null` beim normalen Duell.
- Der Malus geht als **negativer** `zusatzFuerVerlierer` in `trageErgebnisEin` – der `Math.max(0, …)`-Clamp greift dort deshalb **am Ende** der Rechnung, sonst könnte der doppelte Abzug jemanden ins Minus spielen.
- Der **Cooldown-Key ist derselbe** wie beim normalen Duell (`pruefeUndSetzeCooldown`, von allen Duell-Befehlen geteilt) – sonst könnte man abwechselnd über mehrere Befehle spammen.

### Taktikduell (`/pingpong taktikduell`, seit 2026-07-12, war README-Todo)
- **Ebenfalls ein eigener Subcommand** (User-Wunsch, wie beim Ansage-Duell): Schere-Stein-Papier im Ringschluss, `SCHLAEGT`-Map = Schmetterball→Lupfer→Konter→Schmetterball. `entscheideTaktik(herausforderer, gegner)` (exportiert + getestet) liefert `'herausforderer' | 'gegner' | 'gleich'`; bei **gleicher Aktion** fällt es auf den 50/50-`spieleDuell()` zurück (Satz wird dann mit angezeigt). Punkte und Siegesserie laufen unverändert.
- Zustandslos wie die anderen Duelle, aber mit einem **Kniff**: die Aktion des Herausforderers steht in der `customId` (`pingpong-taktik:{gegnerAktion|ablehnen}:{herausfordererId}:{gegnerId}:{herausfordererAktion}`), die des Herausgeforderten ergibt sich daraus, **welchen der drei Aktions-Buttons er klickt** – deshalb steht sie an der Stelle, an der die anderen Duelle `annehmen` haben. Vierter Button = Ablehnen.
- Eigener Einstiegspunkt `handleTaktikButton` (in `interaction.handler.ts` **zusätzlich** zu `handleDuellButton` verkabelt, jeder prüft sein Prefix selbst) – der Ergebnis-Aufbau unterscheidet sich zu stark, um ihn in `handleDuellButton` zu quetschen. Die geteilte Punkte-/Serien-Auswertung liegt in `trageErgebnisEin(siegerId, verliererId, bonusFuerSieger?, bonusFuerVerlierer?)`, die **alle drei Duell-Arten** benutzen.
- Bewusst hingenommen: die verdeckte Wahl ist im Client unsichtbar, aus dem API-Payload aber lesbar – für eine Community-Spielerei okay (stand schon so im README-Todo).

## Blåhaj-Rechner-Feature (`/blahaj` + Auto-Listener)

- Zweck (seit 2026-07-07, Community-Spielerei): Wird im Chat ein Euro-Betrag erwähnt, rechnet der Bot ihn in Blåhajs um (`EURO_PER_BLAHAJ = 28`) und summiert **alle je erwähnten Beträge** serverweit zu einer „Blåhaj-Fläche" in ha (`HECTARES_PER_BLAHAJ = 0.00003`). Beide Konstanten oben in `blahaj.handler.ts`, leicht anpassbar, falls sich der Preis ändert.
- **Zwei Einstiege:** (1) Auto-Listener auf `MessageCreate` – reagiert auf User-Wunsch **bei jeder** €-Erwähnung mit einer Antwort. (2) `/blahaj [betrag]` – ohne Betrag die Server-Gesamtsumme + Fläche, mit Betrag ein reiner Rechner. **Der `/blahaj betrag:`-Wert zählt bewusst NICHT zur Gesamtsumme** – nur der Auto-Listener akkumuliert (sonst wäre der Rechner nicht gefahrlos nutzbar).
- **Bot-Nachrichten werden zwingend ignoriert** (`message.author.bot`): sonst würde die „28 €" in der eigenen Antwort den Listener endlos erneut triggern. Das ist kein Nice-to-have, sondern verhindert eine Schleife.
- Verkabelung: eigener `client.on(Events.MessageCreate, …)` in `index.ts` (neben den Registrierungen von Sport-Auto-Erfassung und Logging-Cache), bewusst getrennte Zuständigkeit wie bei den doppelt registrierten Logging-Events. Async void-Callback → `.catch()` (siehe Stolperfalle unhandled rejection). `blahaj.handler.ts` importiert `client` nicht (arbeitet nur mit `message`/`interaction`).
- Speicher: ein einzelner Redis-Float-Zähler `BLAHAJ:TOTAL_EUR` via neuem `redisService.incrementFloat` (`INCRBYFLOAT`, gibt String → `parseFloat`). Kein Verlauf pro User/Nachricht, nur die Summe.
- Parsing (`parseEuroAmounts`, exportiert + getestet): erkennt `50€`/`€50`/`50 €`/`50 Euro`/`50 EUR` inkl. deutschem Zahlenformat (Komma = Dezimal, Punkt = Tausender, mit Heuristik für alleinstehende Punkte). **Fragil by design** bei exotischen Formaten (z.B. „3€50" → nur 3) – bewusst simpel gehalten (Hobby-Spielerei), im Zweifel Tests erweitern.
- `/blahaj` ist ein **flacher** Command (kein Subcommand) → wie `/news`/`/version` nur in `/hilfe` dokumentiert (nicht per eigenem `hilfe`). `hilfe.handler.test.ts` sichert per `it.each` ab, dass `/blahaj` im `HELP_TEXT` steht.

## Anstupser-Abo (`/anstupser`, seit 2026-07-26, war README-Todo)

- Zweck: Wer will, bekommt täglich um **13:37** (lokale Host-Zeit) eine **DM** vom Bot („Er brauchts mal wieder"). Gruppen-Command `an`/`aus`/`status`/`hilfe`, bewusst **für alle offen** (kein Admin) – jede:r meldet sich selbst an und ab.
- **Opt-in ist die tragende Design-Entscheidung**, nicht bloß Bequemlichkeit: der Bot schreibt niemanden ungefragt per DM an, und die DM-Schwäche (Discord verweigert Bot-DMs mit `50007`, wenn „Direktnachrichten von Servermitgliedern" aus ist) trifft damit nur Leute, die sich aktiv angemeldet haben.
- **Die Test-DM bei der Anmeldung ist der Kern von `handleAn`:** erst senden, dann abonnieren. Scheitert sie, wird **gar nicht erst abonniert** und die (ephemere) Antwort sagt sofort, dass die DMs zu sind – sonst wartet jemand wochenlang still auf nichts. Wer seine DMs später öffnet, ruft einfach `/anstupser an` erneut auf.
- **Scheduling über den vorhandenen 60-s-`setInterval` in `index.ts`** – kein zweiter Mechanismus, keine Cron-Dependency. Der Timer stupst nur an, `sendeAnstupser()` entscheidet selbst: erst `istAnstupserZeit` (`13:37`, exportiert + getestet), dann der Tagesmarker `ANSTUPSER:LAST_DAY`. **Reihenfolge ist Absicht:** die Zeitprüfung steht **vor** dem Marker, damit ein **verpasster Tag NICHT nachgeholt** wird (anders als beim Mitternachts-Kilometerstand – ein Anstupser um 15 Uhr wäre sinnlos). Der Marker verhindert nur den Doppelversand innerhalb derselben Minute.
- **Der Tagesmarker wird VOR dem Versand gesetzt**, nicht danach: bei vielen Abonnenten dauert die Runde, und ein zweiter Timer-Durchlauf würde sie sonst erneut starten. Kehrseite bewusst hingenommen: bricht der Prozess mitten in der Runde ab, bekommen die Restlichen an dem Tag nichts mehr.
- Teilnehmer als Redis-**Set** (`ANSTUPSER:ABOS`, nur User-IDs) – dadurch sind An- und Abmelden von sich aus idempotent, kein Doppel-Eintrag möglich. `istAbonniert` nutzt `sIsMember` statt die ganze Liste zu holen. **`redisService.isSetMember` castet auf `Boolean`** – node-redis liefert dort 0/1, nicht `true`/`false`.
- Zustellung pro Person gefangen (`sendeDm` gibt `false` statt zu werfen): eine geschlossene DM darf die DMs der anderen nicht abbrechen. Nach wiederholtem `50007` wird **nicht** automatisch abgemeldet – bewusst nur `console.warn` (Hobby-Scope, die Person kann ihre DMs jederzeit öffnen). Ein `50007` ist deshalb `warn`, jeder andere Fehler `error`.
- `anstupser.handler.ts` importiert `client` (für `users.fetch`) **nur in Methodenkörpern** – der Handler ist über `commands/index.js` erreichbar, sonst greift die Zirkular-Import-Falle.

## Morgengruß-Tradition (Auto-Listener + `/config`-Button, seit 2026-07-21, war README-Todo)

- Zweck (Community-Spielerei): Die **erste Nachricht jeder Person am Tag** im konfigurierten Morgengruß-Kanal wird vom Bot **nur per Reaktion** begrüßt (👋 + persönliches Emoji) – kein eigener Post, Vorbild ist der Blåhaj-/Sport-Auto-Listener. Bewusst **simpel**: egal welcher Inhalt, **kein Schlüsselwort, kein Uhrzeitfenster**. **Pro Person, nicht global:** jede:r wird an der *eigenen* ersten Nachricht des Tages begrüßt, nicht nur wer als Erste:r überhaupt schreibt (seit 2026-07-25 – vorher ein globaler Tagesmarker; das war das Missverständnis).
- **Emojis sind hier funktional, nicht dekorativ** (die einzige Rückmeldung) – bewusste Ausnahme zur Emoji-Sparsamkeit, wie die ✅-Quittung der Sport-Auto-Erfassung.
- **Person→Emoji zweistufig: gelernt vor abgeleitet** (User-Wunsch, statt willkürlichem Hash). (1) **Gelernt aus der Historie:** `werteReaktionenAus(nachrichten)` (rein, exportiert + getestet) wertet vergangene Nachrichten aus – Signal der Tradition: auf einer Begrüßung von Person X liegt ein 👋 **plus** X's persönliches Emoji, also zählen nur Nachrichten mit 👋, und das **häufigste Ko-Emoji ≠ 👋** je Autor:in gewinnt (Tie-Break deterministisch über den Emoji-String, Schwelle `MIN_BEOBACHTUNGEN = 1`). Ergebnis liegt im Redis-Hash `GREETING:EMOJI` (userId→Emoji). (2) **Fallback `ableiteEmoji(userId)`** (exportiert + getestet): deterministischer Hash in den `GRUSS_EMOJIS`-Pool, wenn nichts gelernt wurde. `handleMessage` liest erst die gelernte Map (fehlertolerant `?? {}`), dann den Fallback.
- **Emoji-Extraktion:** je Reaktion `emoji.id ?? emoji.name` – Custom-Emojis als ID (wieder reaktionsfähig), Unicode als Zeichen (👋 = `WELLE`). Historie wird paginiert gescannt (`SCAN_LIMIT = 500`, 100/Fetch); die aus der Historie geholten Nachrichten tragen ihre Reaktionen schon im Payload, **kein Nachladen der Reagierenden nötig**.
- **Kanal-Setzen UND Lernen via `/config` – `/morgengruss` komplett entfernt (seit 2026-07-26):** Der Morgengruß-Kanal wird über `/config` gesetzt, das Auffrischen der gelernten Emojis stößt ein **Button** im Morgengruß-Bereich der Seite an (POST `/config/morgengruss` → `handleMorgengrussLernen` → `greetingHandler.lerneAusHistorie()`; Rückmeldung „X Emojis gelernt" per Post/Redirect/Get, `?gelernt=N` bzw. `?morgengruss=kein-kanal`). Damit gibt es **keinen `/morgengruss`-Slash-Command mehr** (Command-Datei + Test + `commands/index.ts`-Eintrag entfernt). Historie: 2026-07-25 war der Kanal schon auf `/config`, das Lernen lief noch über `/morgengruss lernen` (die Entkopplung – Kanal setzen vs. scannen – war nötig, weil der Scan auf einer Redirect-Seite schlecht abbildbar schien); 2026-07-26 wurde auch das Lernen als Button auf die Seite gezogen und der Command ganz entfernt. **`lerneAusHistorie()`** (Ersatz für das frühere `handleLernen`) löst den Kanal selbst auf und gibt die Anzahl gelernter Zuordnungen zurück, oder **`null`**, wenn kein (abrufbarer) Kanal gesetzt ist (der Router leitet dann auf den kein-Kanal-Hinweis). Als reine Admin-Aktion **nicht** im `HELP_TEXT`; der Auto-Gruß selbst läuft ohnehin ohne user-invokebaren Befehl. **XSS-Falle beim Rückmelde-Redirect:** `?gelernt=` wird in `handleConfigPage` bewusst zu `Number` gecastet (nicht roh interpoliert), sonst wäre die Query ein XSS-Vektor.
- **Emoji-Übersicht auf einer EIGENEN Seite `/config/morgengruss-emojis` (seit 2026-07-26, war README-Todo):** Tabelle **Name → Emoji → Herkunft → Ändern** (`renderMorgengrussEmojiSeite`/`renderMorgengrussEmojis` im Router, Daten aus `holeMorgengrussEmojis` in `config.settings.ts`). **Ausgelagert, weil die Tabelle mit der Mitgliederzahl wächst** und `/config` sonst dominiert – Muster wie `/config/logs`. Im Morgengruß-Bereich steht nur noch ein Link samt Anzahl (`renderMorgengrussEmojiLink`); die Hauptseite lädt die Emoji-Daten dadurch **gar nicht mehr** (zwei Redis-Reads weniger pro `/config`-Aufruf, ein Test nagelt das fest). Der Speichern-Redirect geht auf **die Unterseite** zurück (`/config/morgengruss-emojis?gespeichert=1`), nicht auf `/config` – man bleibt, wo man gearbeitet hat; deshalb braucht diese Aktion auch keinen `MELDUNGEN`-Eintrag. Grund: die gelernte Zuordnung war nirgends einsehbar – der Lern-Button meldet nur „X Emojis gelernt", welches Emoji wer bekam, zeigte sich erst am nächsten Morgen. Drei Entscheidungen dahinter: (1) **Custom-Emojis liegen als blanke Snowflake-ID im Hash** (`emoji.id ?? emoji.name`, siehe `werteReaktionenAus`) und werden über `guild.emojis.cache.get(id)` aufgelöst und als **`<img>`** gerendert – Discord-Markup wie `<:name:id>` rendert **nur in Discord**, auf einer Webseite stünde dort nichts bzw. eine Zahl. Eine ID ohne Treffer (Emoji vom Server gelöscht) wird als „gelöscht" markiert, nicht als leere Zelle verschluckt – mit so einem Emoji würde `message.react` beim Gruß scheitern. (2) Gelistet werden **alle** Mitglieder, nicht nur die gelernten (`ableiteEmoji` greift ohnehin für jede:n), aber die Spalte „Herkunft" trennt `gelernt` von `abgeleitet` – ohne sie sähe der Fallback wie eine echte Zuordnung aus, mit nur den gelernten fehlte die halbe Belegschaft. Die Datenform ist eine unterschiedene Union (`MorgengrussEmoji`: `unicode` | `custom` | `unbekannt`) statt fertigem HTML – Sammeln bleibt in `config.settings`, Rendern im Router (dokumentierte Trennung).
- **Bearbeitbar per FREITEXT-Feld** (war zunächst read-only, dann kurzzeitig ein Dropdown): je Zeile ein `<input type="text">` + Speichern gegen `POST /config/morgengruss-emoji` (`handleMorgengrussEmojiSpeichern`, Reihenfolge: CSRF → Mitglied-Whitelist → `deuteEmojiEingabe` → speichern → Redirect). **Das Dropdown war der falsche Weg und ist am 2026-07-26 wieder rausgeflogen:** eine geschlossene Auswahl (Pool + Server-Emojis + Vergebenes) kannte gängige Emojis wie 🍪 schlicht nicht, und genau die will man setzen. Die Liste lebt als **`<datalist>`** weiter – Vorschläge zum Anklicken (`holeEmojiVorschlaege`), aber **keine Einschränkung** der Eingabe. Ein `<datalist>` reicht einmal für alle Zeilen; je Zeile eines wäre dieselbe Liste dutzendfach im HTML.
- **`deuteEmojiEingabe(eingabe)` ist die Validierung** (rein, exportiert + getestet) und gibt zurück, was gespeichert wird, oder `null`. Drei akzeptierte Formen: `:name:` (Server-Emoji, wird **zur ID aufgelöst**), `<:name:id>`/`<a:name:id>` (was Discord beim Kopieren einfügt) und das Emoji selbst. Unicode-Prüfung über `\p{Extended_Pictographic}`/`\p{Emoji_Component}`/`\p{Regional_Indicator}` plus ZWJ/VS16, damit **zusammengesetzte** Emojis (👩‍💻, 👍🏽, 🇩🇪) durchgehen; zusätzlich muss mindestens ein „echtes" Emoji-Zeichen dabei sein (`\p{Emoji_Component}` allein wäre auch eine blanke Ziffer) und die Länge ≤ 16 UTF-16-Einheiten liegen. Server-Emojis werden **gegen die Guild geprüft** – ein erfundenes `:name:` würde beim Gruß still an `message.react` scheitern, das soll hier auffallen. Freitext heißt also *nicht* beliebig: normaler Text, mehrere Emojis oder Buchstaben werden mit 400 abgelehnt.
- **Das Eingabefeld zeigt die tippbare Form** (`eingabeWert`, Umkehrung von `deuteEmojiEingabe`): Unicode-Emoji direkt, Server-Emoji als `:name:` (keine ID abtippen), und bei einer **kaputten Zuordnung leer** – dort gibt es nichts sinnvoll vorzugeben, und ein leeres Pflichtfeld erzwingt eine bewusste Neueingabe.
- **Kein „Zurücksetzen auf abgeleitet"**: einmal von Hand gesetzt, gilt der Eintrag als `gelernt`. Praktisch kein Verlust, weil der abgeleitete Wert selbst im Pool steht und wieder wählbar ist – nur die Herkunft-Spalte sagt dann „gelernt" statt „abgeleitet".
- **Doppelgruß-Schutz pro Person:** Redis-**Hash** `GREETING:LAST_DAY_BY_USER` (Feld = userId → YYYY-MM-DD, lokales Berlin-Datum), `getLastGreetingDay(userId)`/`setLastGreetingDay(userId, tag)` in `greeting.service.ts`. **Bewusst umbenannt (2026-07-26, Bugfix):** bis 1.51.x war `GREETING:LAST_DAY` ein einfacher **String** (globaler Tagesmarker); mit dem Wechsel auf einen Hash pro Person (1.52.0) lief `HGETALL` gegen diesen alten String-Key → **WRONGTYPE**, `getLastGreetingDay` warf, der Handler brach ab → **gar kein Gruß mehr** (und ein WRONGTYPE-Log je Nachricht im Kanal). Neuer Key-Name = keine Kollision; der verwaiste String-Key liegt harmlos brach. Lehre: Ändert sich der Redis-**Typ** eines bestehenden Keys, muss der Key-Name mitwandern (oder der alte gelöscht werden) – sonst crasht der erste Zugriff auf Bestandsdaten. Der Handler beansprucht den Tag **für die Person** vor dem Reagieren – ein winziger Race bei zwei fast gleichzeitigen Nachrichten derselben Person ist möglich, aber harmlos (nur eine Extra-Reaktion); bewusst kein atomares SET NX für eine Hobby-Spielerei. Reaktionen sind **best-effort** (je eigenes `.catch()`): scheitert die Welle, kommt trotzdem das persönliche Emoji.
- **Bot-Nachrichten MÜSSEN ignoriert werden** (`message.author.bot`) – sonst könnte ein Bot-Post als erste Nachricht einer „Person" durchgehen.
- Verkabelung: eigener `client.on(Events.MessageCreate, …)` in `index.ts` (neben Blåhaj/Sport/Logging), async void → `.catch()`. `greeting.handler.ts` importiert `client` (nur in Methodenkörpern → Zirkular-Import-Falle umgangen, wie `twitch`/`sport.handler.ts`), da `lerneAusHistorie()` den konfigurierten Kanal über `client.channels.fetch` auflöst. `config.router.ts` importiert `greetingHandler` für den Lern-Button (kein Zyklus: greeting.handler importiert config.router nicht). Neuer State (`GREETING:CHANNEL`/`LAST_DAY_BY_USER`/`EMOJI`) in `docs/datenhaltung.md`.

## Diagnose-Feature (`/diagnose`, seit 2026-07-21, war README-Todo)

- Zweck: **feature-übergreifender** Admin-Gesundheits-/Konfigurations-Check – zeigt auf einen Blick, ob alle setzbaren Kanäle/Einstellungen **gesetzt UND abrufbar** sind. **Ersetzt** das frühere `/twitch diagnose` (Subcommand + `twitchHandler.handleDiagnose` entfernt, siehe Twitch-Feature). Flacher Command (`diagnose.command.ts` → `diagnose.handler.ts`), Antwort **ephemer**.
- Geprüft wird (alle Keys in `docs/datenhaltung.md`): Twitch-Benachrichtigungskanal + -rolle **und** EventSub-Subscriptions nach Status (nur `enabled` stellt zu), Sport-Ankündigungskanal, Protokoll-Kanal, Morgengruß-Kanal, ob ein Event gesetzt ist, und die **verknüpften LotGD-Charaktere** (`pruefeCharaktere`, seit 2026-07-22): pro Verknüpfung wird per `findInRoster` geprüft, ob der Name **noch im aktuellen Roster** steht (Suffix-Match wegen Titel-Präfix) – ein fehlender Treffer ist ein **verwaister Link** (Charakter umbenannt/gelöscht), am Ende als Anzahl zusammengefasst. Je Kanal drei Zustände über den geteilten Helper `pruefeKanal`: **❌ nicht gesetzt** / **⚠️ gesetzt, aber nicht abrufbar** (`client.channels.fetch` gegengeprüft – fängt gelöschte Kanäle / fehlenden Zugriff ab) / **✅ ok**. Die ✅/⚠️/❌-Marker sind hier **funktional** (Admin-Debugging, dokumentierte Emoji-Ausnahme), nicht dekorativ.
- **Keine Testnachricht mehr** (bewusst weggelassen): eine allgemeine Diagnose würde sonst mehrere Kanäle vollposten – die reine Abrufbarkeitsprüfung reicht.
- **Config-Seite-Check (`pruefeConfig`, seit 2026-07-25):** prüft, ob der `/config`-Login scharf geschaltet ist (`oauthConfigured()` + `sessionConfigured()` – beide Secrets gesetzt). Reines Config-Lesen, **kein** externer Ping: die externe Erreichbarkeit (nginx/Web-Backend-Mapping) wird bewusst nicht geprüft, das bräuchte einen Selbst-HTTP-Request und deckt der Deploy-`curl` (`/twitch/eventsub` → 404) ohnehin ab. Fängt die häufigste „warum geht /config nicht"-Ursache (vergessenes Secret → 503). **Nennt seit 2026-07-26 auch die Adresse** (`CONFIG_BASE_URL` + `/config`) – die stand sonst nirgends im Bot (die Seite hat bewusst keinen eigenen Command), Admins mussten sie auswendig kennen.
- **Twitch-EventSub-Teil (`pruefeEventSub`) und Charakter-Teil (`pruefeCharaktere`) sind fehlertolerant** (eigenes try/catch): ein Twitch-Ausfall, ein nicht abrufbares Roster oder ein Redis-Problem bei den Verknüpfungen darf den Rest der Diagnose nicht kosten – dann steht dort nur eine Warnzeile. Der Charakter-Teil nutzt das gecachte `characterService.getRoster()` (kein zusätzlicher Fetch, wenn `/charakter`/`/online` es kürzlich geholt hat).
- `diagnose.handler.ts` importiert `client` nur in Methodenkörpern (Zirkular-Import-Falle umgangen). Als reiner Admin-Command **nicht** im `HELP_TEXT` (wie `/protokoll`/`/rollenbutton`).

## Web-Konfigurationsseite (`/config`, seit 2026-07-23, war README-Todo „Verwaltungs-/Einstellungsseite")

- Zweck: eine (Web-)Oberfläche für die Einstellungen, die bisher nur über Admin-Commands gehen. `GET /config` zeigt alle Admin-Einstellungen nach Bereich gruppiert **direkt zum Bearbeiten** (Twitch-Benachrichtigungskanal + -rolle, Sport-Ankündigungskanal + Kilometer-Admin, Protokoll-Kanal, Morgengruß-Kanal + Lern-Button, nächstes Event).
- **Die read-only-Übersichtstabelle ist am 2026-07-26 entfallen** (`sammleEinstellungen`/`renderEinstellungen`/`Einstellung` gelöscht, `EinstellungStatus` → `FeldStatus`): sie zeigte exakt dieselben sechs Werte, die die Formulare darunter schon als `selected` im Dropdown tragen, und las sie dafür **ein zweites Mal aus Redis** (4 Kanäle + Rolle + Event = 6 doppelte Reads pro Seitenaufruf). Ihr einziger echter Mehrwert war der **Zustand** – der steckt jetzt am jeweiligen Feld: `KanalFeld.status`/`RollenFeld.status` (`FeldStatus` = `ok` | `warnung` | `leer`), erzeugt in `ladeKanalFelder`/`holeTwitchRolle`. `handleConfigPage` holt alles per **`Promise.all`** (die Abfragen hängen nicht voneinander ab).
- **Dabei fielen zwei Anzeigefehler auf, die die Tabelle verdeckt hatte:** ein `<select>` selektiert immer irgendetwas, also stand bei **nicht gesetzt** einfach der erste Kanal im Feld (sah aus wie konfiguriert), und bei einem **gelöschten** Kanal fand die gespeicherte ID keine Option, das Feld sprang auf den ersten Eintrag (kaputter Zustand unsichtbar). Fix ist `platzhalterOption`: bei `leer`/`warnung` eine **`disabled selected`**-Option („— nicht gesetzt —" bzw. „— gesetzt (ID), aber nicht abrufbar —") plus `required` am `<select>` – `disabled` wird nicht mitgeschickt und ist nicht wieder wählbar, erzwingt also eine echte Wahl. **Ausnahme Rolle:** dort ist „— keine —" ein *gültiger* Wert (Rolle entfernen), deshalb kein `disabled`/`required`, der Platzhalter kommt nur bei `warnung`.
- **Bearbeiten der Kanal-Einstellungen (seit 2026-07-25):** Ein Bearbeiten-Formular je Kanal – Protokoll-, Twitch-Benachrichtigungs-, Sport-Ankündigungs- und Morgengruß-Kanal. Alle posten an **eine** Route `POST /config/kanal` mit einem hidden `feld` (welche Einstellung) + `kanal` (die ID). **Datengetrieben:** `KANAL_SERVICES` in `config.settings.ts` mappt `schluessel` → `{label, lade, speichere}` (Redis-Getter/-Setter des jeweiligen Service); ein neues Kanal-Feld ist damit **eine Zeile**, keine neue Route/Handler. `ladeKanalFelder` liefert Feld+aktuelle ID (Vorauswahl), `istKanalFeld` ist die Feld-Whitelist (`hasOwnProperty`, damit `__proto__` o.ä. nicht durchrutscht), `speichereKanal(schluessel, id)` schreibt.
- Muster je Formular: **Kanal-Dropdown** statt Freitext-ID (`holeTextKanaele` = Text-/Announcement-Kanäle des Servers, dient zugleich als **Kanal-Whitelist** `istGueltigerTextKanal`; sortiert nach Name **ohne führendes Emoji**, s. `sortierschluessel`), verstecktes **CSRF-Token** (`createCsrfToken`/`verifyCsrfToken` in `config.session.ts` – zustandslos aus der User-ID per HMAC, kein Formular-State). Handler-Reihenfolge **CSRF → Feld-Whitelist → Kanal-Whitelist → speichern → Redirect** (`/config?gespeichert=1`, Post/Redirect/Get gegen Doppel-Speichern beim Reload). `requireConfigAuth` legt die User-ID in `res.locals.configUserId` ab, damit der nachgelagerte Handler das CSRF-Token bauen/prüfen kann.
- **Twitch-Benachrichtigungsrolle (seit 2026-07-25):** eigenes Formular + Route `POST /config/rolle` (Rollen- statt Kanal-Dropdown). Zwei Unterschiede zum Kanal: die Rolle ist **optional** → das Dropdown hat eine **„— keine —"-Option** (leerer Wert), und ein leerer Wert im Handler bedeutet **entfernen** (`speichereTwitchRolle(null)` → neuer `twitchUserService.removeNotificationRole()` via `redisService.delete`). `holeRollen` liefert die Rollen des Servers **ohne @everyone** (`id === guild.id`) als Whitelist (`istGueltigeRolle`). Sonst identisches Muster (CSRF → Whitelist → speichern → Redirect).
- **Event bearbeiten (seit 2026-07-25):** eigenes Formular + Route `POST /config/event`. Kein Dropdown, sondern **native `<input type="date">` + `<input type="time">` (optional) + Titel-Textfeld**. `parseIsoDateTime(datum, uhrzeit)` (in `config.router.ts`, exportiert+getestet) baut den Timestamp mit **Round-Trip-Check** (wie `parseGermanDateTime`, damit z.B. `2026-02-31` nicht still normalisiert), der Handler prüft zusätzlich **Vergangenheit** (`<= Date.now()` → 400). **Zwei Aktionen in einem Formular** über `name="aktion"` (speichern/entfernen); der Entfernen-Button ist `formnovalidate`, damit er trotz `required`-Datum abschickbar ist. `holeEventFelder`/`speichereEventDaten`/`entferneEvent` in `config.settings.ts` (→ `eventService`).
- **Body-Parser NUR an den POST-Routen**, **nicht** global: global gemountet würde er den Twitch-Rohkörper zerstören und dessen Signaturprüfung brechen. Absicherung: der Twitch-Integrationstest (`npm run test:twitch`) muss weiter `204` liefern. Auth läuft **vor** dem Body-Parser, damit für Unbefugte gar nichts geparst wird. Damit sind **alle Einstellungen, die `/config` anzeigt, auch dort bearbeitbar**; global bleibt tabu.
- **Routen-Verkabelung über drei Helfer (seit 2026-07-26)** – vorher stand der identische Auth-plus-`.catch`-Block **sechsmal** wortgleich da (~90 der damals 834 Zeilen): `geschuetzt` (async `requireConfigAuth` mit `.catch`), `fangeFehler(handler, kontext, meldung)` (async Handler mit `.catch` → 500; ohne das killt eine unhandled rejection den Prozess, siehe Stolperfallen) und **`postRoute(pfad, handler, kontext)`**, das die drei Zutaten einer POST-Route in der richtigen Reihenfolge bündelt: `geschuetzt` → `urlencoded` → Handler. **Neue POST-Route = eine Zeile `postRoute(...)`** – dadurch kann man den Body-Parser nicht mehr vergessen und, wichtiger, nicht mehr versehentlich global mounten.
- **Kein Caching, keine Indexierung:** `setzeAdminHeader` (auf `/config` und alles darunter per `configRouter.use`) setzt `Cache-Control: no-store` (sonst zeigt der Back-Button nach dem Abmelden noch die Einstellungen) und `X-Robots-Tag: noindex, nofollow` (die Login-Seite ist öffentlich erreichbar). Benannt exportiert wie alle Handler, damit der Test ihn direkt aufrufen kann – **es gibt kein supertest im Projekt**, Routen werden nie durch Express getestet, sondern die Handler einzeln mit Mock-`req`/`res`.
- **POST ohne gültige Session gibt `401`, nicht `200`+Login-Seite** (`antworteNichtAngemeldet` verzweigt über `req.method`): bei GET ist die Login-Seite die normale Einstiegsseite, bei POST hat jemand gerade ein Formular abgeschickt und die Eingaben sind weg – ein `200` mit Login-HTML sähe aus, als hätte das Speichern geklappt. Die Antwort sagt deshalb ausdrücklich, dass **nicht** gespeichert wurde.
- **Labels sind mit ihren Feldern verknüpft** (`<label for>` + `id`, seit 2026-07-26): ohne das liest ein Screenreader das Feld unbeschriftet vor und der Klick aufs Label fokussiert nichts. IDs: `kanal-<schluessel>`, `twitch-rolle`, `sport-mitglied`, `sport-altkilometer`. Beim Morgengruß-Lernen steht bewusst ein **`<span class="pseudo-label">`** statt `<label>` – dort gibt es kein Eingabefeld, und ein `<label>` für einen Button ist ungültiges HTML.
- **Die ersetzten Admin-Slash-Commands wurden entfernt (seit 2026-07-25):** `/protokoll` (ganz), `/twitch benachrichtigungskanal` + `benachrichtigungsrolle`, `/sport ankuendigungskanal`, `/morgengruss` (komplett) – ihre Werte/Aktionen laufen jetzt ausschließlich über `/config`. Die zugehörigen Handler-Methoden (`loggingHandler.handleSetChannel`, `twitchHandler.handleBenachrichtigungskanal`/`-rolle`, `sportHandler.handleAnkuendigungskanal`, `greetingHandler.handleSetChannel`) und die `log.command.*`/`morgengruss.command.*` sind mit weg; die **Service-Setter bleiben** (config.settings nutzt sie). **Morgengruß (finaler Stand seit 2026-07-26):** Kanal-Setzen **und** Emoji-Historien-Scan sind beide auf `/config` – der Scan als Button (POST `/config/morgengruss` → `greetingHandler.lerneAusHistorie()`). Zwischenstand 2026-07-25 (Kanal auf `/config`, Scan noch per `/morgengruss lernen`) ist damit abgelöst; `handleLernen` wurde durch `lerneAusHistorie()` ersetzt, der `/morgengruss`-Command ganz entfernt.
- **HTML-Escaping ist Pflicht** (`escapeHtml` in `config.router.ts`): sobald dynamische Werte (Kanal-/Rollen-Namen, Event-Titel aus User-/Discord-Daten) in die Seite interpoliert werden, wäre das sonst ein XSS-Vektor. Jede Render-Funktion escaped ihre Werte, Tests nageln das fest. Datenfluss bewusst getrennt: `config.settings.ts` sammelt (Services + Discord-Auflösung, testbar mit Mocks), `config.router.ts` rendert (reine Funktionen). Neue Werte gehören nach `config.settings.ts`, neue Darstellung nach `config.router.ts`.
- **Rückmeldung nach dem Speichern (seit 2026-07-26):** Statt `?gespeichert=1` → anonymes „Gespeichert." am Seitenanfang trägt der Redirect jetzt einen **Aktions-Schlüssel + Bereichs-Anker** (`/config?gespeichert=kilometer-setzen#bereich-sport`, gebaut von `zurueckZumBereich`). `leseMeldung(req)` übersetzt den Schlüssel über die feste Tabelle **`MELDUNGEN`** (Schlüssel → `{bereich, text}`) in eine `Meldung`, die `renderBereich` **im betroffenen Bereich** ausgibt – man landet also dort, wo man geklickt hat, und liest, *was* passiert ist (vorher bei zehn Formularen auf der Seite nicht erkennbar). **Der Query-Wert wird nur als Schlüssel benutzt, nie ausgegeben** (Lookup per `hasOwnProperty` wie `istKanalFeld`, damit `?gespeichert=constructor` nicht in der Prototypenkette landet); der Meldungstext wird zusätzlich escaped. Der Morgengruß-Lern-Button behält seine eigenen Parameter (`?gelernt=N` mit `Number`-Cast / `?morgengruss=kein-kanal` als Warnung), weil seine Meldung eine Zahl trägt bzw. keine Erfolgsmeldung ist. Neue Aktion = eine Zeile in `MELDUNGEN` + `zurueckZumBereich(res, schluessel)`.
- **Sport-Admin auf /config (seit 2026-07-25):** In der Sport-Karte zusätzlich „Kilometerstand eines Mitglieds setzen" (Mitglied-Dropdown = Whitelist via `holeMitglieder`/`istGueltigesMitglied`, ohne Bots), „Bestandskilometer" (Addieren/Setzen, 0 = entfernen) und die **Meilenstein-Liste** mit je einem Entfernen-Button (`renderMeilensteinListe`; **Anlegen bleibt der Command `/sport meilenstein setzen`** – bewusst für ALLE offen). Route `POST /config/sport`, Dispatch über `name="aktion"` (`kilometer-setzen`/`altkilometer-addieren`/`altkilometer-setzen`/`meilenstein-entfernen`), Kilometer als Zahl ≥ 0 validiert. **Das Kilometer-Setzen ist die gefährlichste Aktion der ganzen Seite** und hat deshalb seit 2026-07-26 drei Bremsen: es überschreibt den Stand einer anderen Person dauerhaft und legt (wie der frühere `/sport setzen`) **keinen `SportEntry`** an, ist also auch per `/sport bearbeiten` nicht zu reparieren. Bremsen: (1) `holeMitglieder()` ist **async** und hängt über `sportService.getAlleKilometer()` (ein Redis-Read für alle) den **aktuellen Kilometerstand ans Dropdown-Label** (`Tirsis (128.5 km)`) – vorher setzte man blind; Anzeige über `formatKilometerstand` auf 2 Nachkommastellen (Float-Reste), bewusst **nicht** `rundeKilometer` (ganze km), ein Admin korrigiert einen konkreten Wert. (2) Vorausgewählt ist **nichts** (leere `required`-Option „— Mitglied wählen —" statt des alphabetisch Ersten – ein Fehlklick traf sonst irgendwen). (3) `confirm()` am Button, ebenso beim Meilenstein-Entfernen (der einzige Einsatz von Inline-JS auf der Seite, bewusst nur an den beiden datenvernichtenden Aktionen). Die **Whitelist** `istGueltigesMitglied` nutzt weiterhin die synchrone interne `guildMitglieder()` (kein Redis im Validierungspfad). Die migrierten Slash-Commands (`/sport setzen`/`altkilometer`/`altkilometer-setzen`/`meilenstein liste`/`entfernen`) samt Handler-Methoden sind **entfernt**; die Service-Setter bleiben (config.settings nutzt sie).
- **Fachliche Gruppierung (seit 2026-07-25):** `renderConfigSeite` ordnet die Bearbeiten-Formulare nach Feature in `<fieldset class="bereich">`-Blöcke (`renderBereich`, `<legend>` = Bereichsname): **Twitch** (Kanal + Rolle), **Sport**, **Nachrichten-Protokoll**, **Morgengruß** (Kanal + Lern-Button `renderMorgengrussLernen`), **Event**. Einzelne Kanal-Formulare rendert `renderKanalFormular` (die frühere gebündelte `renderKanalFormulare` ist entfallen). Jeder Bereich hat seit 2026-07-26 eine **`id="bereich-<name>"`** – das ist das Sprungziel des Redirects nach dem Speichern und der Ort, an dem die Meldung erscheint. Das Event-Formular hat ein eigenes einzeiliges Layout (`form.event-form`: date/time/titel + Buttons nebeneinander, ohne Label-Spalte – die `<legend>` „Event" reicht).
- **Log-Ansicht (`/config/logs`, seit 2026-07-26, war README-Todo):** Zeigt die letzten ~500 Zeilen des laufenden Prozesses, damit man bei „irgendwas kommt nicht an" nicht per SSH ins Supervisor-Log muss. **Bewusst In-Memory-Ringpuffer statt Datei** (`logBuffer.service.ts`, `installLogCapture()` patcht `console.log/warn/error` und spiegelt eine redigierte Kopie in den Puffer, Ausgabe geht unverändert weiter): kein Wissen über den (auf Uberspace suffixbehafteten) Logpfad nötig, kein Dateizugriff. Der Nachteil „nach Neustart leer" greift kaum – crasht der Bot, ist `/config` ohnehin tot; läuft er, hat der Prozess die relevanten Zeilen im RAM. `installLogCapture()` wird in `index.ts` **früh** aufgerufen (nach den Imports, vor der Verkabelung), damit Boot-/Laufzeit-Zeilen mitlaufen. **Secret-Redaktion:** bekannte `config.json`-Secrets (≥ 8 Zeichen) werden beim Puffern durch `***` ersetzt (Vorsicht gegen fremde Stacktraces). **`renderLogs` escaped jede Zeile** (Log-Text kann User-/Kanalnamen tragen → sonst XSS). Route mit `requireConfigAuth`; als reine Ausgabe kein Body-Parser/CSRF nötig (GET). Grenzen dokumentiert in `docs/datenhaltung.md`. **Seit 2026-07-26 neueste Zeile zuerst** (der Puffer bleibt chronologisch, `renderLogs` dreht nur die Anzeige) – wer hier nachsieht, sucht „was ist gerade passiert" und musste vorher 500 Zeilen weit scrollen – plus ein **Filter „nur Warnungen und Fehler"** (`?nur=probleme`, blendet `level === 'log'` aus; beide Stände zeigen ihre Trefferzahl, der aktive ist kein Link). Bei viel Normalbetrieb im Log ist das der Unterschied zwischen brauchbar und nicht.
- **Layout-Vorschau ohne Deploy:** die komplette Seite baut die reine Funktion `renderConfigSeite(daten)` (keine Redis/Discord-Zugriffe – `handleConfigPage` sammelt die Daten und ruft sie). `scripts/config-vorschau.ts` (→ `npm run vorschau:config`) ruft `renderConfigSeite` mit Beispieldaten und schreibt `config-vorschau.html` (gitignored) – nutzt die **echten** Render-Funktionen + das echte CSS, also keine Drift zur Live-Seite. Seit der Auslagerung der Emoji-Tabelle schreibt es zusätzlich `config-vorschau-emojis.html` für die Unterseite; dafür ist **`renderPage` exportiert** (die Seiten-Hülle samt CSS) – ohne sie zeigte die Vorschau einen nackten Body und wäre als Layout-Werkzeug wertlos. Zum Iterieren am Layout gedacht: CSS/HTML in `config.router.ts` ändern → `npm run vorschau:config` → Datei im Browser prüfen, statt für jede Optik-Änderung zu deployen. Das Skript importiert aus dem gebauten `dist/` (geht nur, weil `tsconfig.json` `declaration: true` hat → `.d.ts` vorhanden).
- **Ein Server, ein Port:** Der Config-Router hängt an **derselben** Express-App wie der Twitch-Webhook (Port 3000), verkabelt in `index.ts` via `webhookServer.app.use(configRouter)` **vor** `webhookServer.start(3000)`. Kein zweiter `listen`. `config.router.ts` mountet die Routen selbst (`/config`, `/config/login`, `/config/callback`, `/config/logout`).
- **Kein Body-Parser nötig – und das ist kein Zufall:** Discord ruft den OAuth-Callback als **`GET /config/callback?code=…&state=…`** (nur Query-Params). Der Twitch-`express.raw({type:'application/json'})`-Parser ist **nur auf `/twitch`** gemountet und bleibt unberührt. Sobald `/config` Formulare (POST) bekommt, braucht es einen **eigenen `express.urlencoded` NUR auf `/config`** – global gemountet würde er den Twitch-Rohkörper zerstören und die Signaturprüfung brechen.
- **Deployment (Uberspace):** `/config` braucht ein **eigenes Web-Backend-Mapping** (`uberspace web backend`, analog zu `/twitch`), sonst ist der Pfad extern nicht erreichbar. Reiner Deploy-Schritt, kein Code.
- Als reiner Admin-Bereich (kein Slash-Command) **nicht** im `HELP_TEXT`.

### Auth: Discord OAuth2 (`config.session.ts` + `discordOAuth.service.ts` + `config.router.ts`)

- **Login über Discord OAuth2, Scope nur `identify`** – wir wollen ausschließlich die Discord-User-ID. **Der Clou:** die Admin-Prüfung macht **unser eigener Bot**, nicht Discord: `client.guilds.cache.get(config.GUILD_ID)?.members.fetch(userId)` → `member.permissions.has(PermissionFlagsBits.Administrator)` (`pruefeAdmin` in `config.router.ts`). `members.fetch` wirft auch, wenn die Person gar nicht (mehr) auf dem Server ist → im catch `false` (Nicht-Mitglieder kommen nicht rein). **Admin = Administrator-Permission** ist eine bewusste, minimale Wahl; für Rollen-Gating ist es der **Ein-Zeilen-Umschaltpunkt** `member.roles.cache.has(ROLLEN_ID)` (dann müsste eine Rollen-ID in die Config).
- **`pruefeAdmin` läuft bei JEDEM `/config`-Aufruf, nicht nur beim Login** (`requireConfigAuth` ist deshalb async). Bewusste Härtung (Security-Review 2026-07-23): das Session-Cookie allein sagt nur „war mal Admin"; wer die Rolle verliert oder den Server verlässt, wäre sonst bis zum Cookie-Ablauf (7 Tage) noch drin. Bei Verlust der Rechte wird das tote Cookie sofort gelöscht (sonst löst es bei jedem Request erneut ein `members.fetch` aus). Der eine API-Call pro Seitenaufruf ist bei einem Admin-only-Panel vernachlässigbar. Die async Middleware ist in der Route mit `.catch()` verkabelt (Sicherheitsnetz; `pruefeAdmin` fängt Fehler ohnehin selbst → `false`).
- **Session = zustandslos signiertes Cookie**, **kein Redis** (dieselbe Philosophie wie die Button-`customId`s, überlebt Neustarts): `config_session` = `<userId>.<ablaufMs>.<HMAC-SHA256>` (`CONFIG_SESSION_SECRET`), 7 Tage. `signSession`/`verifySession` in `config.session.ts`; die Signatur wird per **`crypto.timingSafeEqual`** geprüft (vorher Längen-Check, sonst wirft `timingSafeEqual`). Cookie-Attribute: `HttpOnly`, `SameSite=Lax`, `Path=/config`, `Secure` nur bei https (aus `CONFIG_BASE_URL` abgeleitet – sonst verwirft der Browser das Cookie auf http-localhost).
- **Cookies werden nativ gebaut/geparst** (`buildSetCookie`/`parseCookies`), **keine `cookie-parser`/`express-session`-Dependency** – minimal-deps-Stil wie das manuelle HTML-Parsing in `news.service`. `fetch` für die Discord-Netzcalls ist Node-global (Host Node 22), auch keine Dependency.
- **CSRF-Token der Formulare haben einen Ablauf** (seit 2026-07-26, Aufbau `<ablauf>.<hmac>` wie `signSession`, `CSRF_MAX_AGE_SECONDS` = Session-Dauer): vorher war es ein reiner HMAC über die User-ID, also für die Lebensdauer des Secrets **unveränderlich** gültig – ein einmal mitgelesenes Token (Screenshot des Seitenquelltexts, Browser-Erweiterung) hätte nie aufgehört zu gelten. Die Dauer ist absichtlich **gleich** der Session, nicht kürzer: ein offener Tab soll nicht vorzeitig in ein 403 laufen, und länger als die Session kann ein Token nichts nützen (ohne gültige Session kommt der Request gar nicht bis zur CSRF-Prüfung). `verifyCsrfToken` prüft den Ablauf **nach** der Signatur, damit ein gefälschter Ablauf schon vorher aussortiert ist. Folge für Tests: das Token ist nicht mehr Zeichen-für-Zeichen vorhersagbar – aus dem HTML lesen und `verifyCsrfToken` fragen, nicht `toContain(createCsrfToken(...))`.
- **CSRF beim Login:** `state`-Parameter als **Double-Submit-Cookie** – `handleLogin` erzeugt `randomUUID`, setzt es als kurzlebiges `config_oauth_state`-Cookie (10 min) und hängt es an die Authorize-URL; `handleCallback` vergleicht Cookie vs. Query-`state` (einfaches `===` reicht für einen Einmal-Nonce; das Session-HMAC ist das timing-safe verglichene Geheimnis).
- **Discord-Netzcalls in `discordOAuth.service.ts` sind fehlertolerant** (`null` statt throw, Muster wie `twitchService.getStreamInfo`): `exchangeCodeForToken` (Code→Access-Token) und `fetchDiscordUserId` (`GET /users/@me`). **Das Access-Token wird nur transient benutzt und nirgends gespeichert**, kein Refresh-Token (siehe `docs/datenhaltung.md`).
- **Fail-closed:** Fehlen `DISCORD_CLIENT_SECRET` oder `CONFIG_SESSION_SECRET`, meldet `authConfigured()` `false` → `requireConfigAuth`/`handleLogin`/`handleCallback` liefern **503** und die Seite bleibt **zu** statt offen. Beide Secrets + `CONFIG_BASE_URL` (optional, Default `http://localhost:3000`) werden per Cast-Muster `(config as {X?: string}).X` gelesen (bricht die inferierte `config.json`-Typisierung nicht). Redirect-URI = `<CONFIG_BASE_URL>/config/callback` – **muss exakt** einem der im Discord Developer Portal hinterlegten Redirects entsprechen (lokal + prod).
- **Zirkular-Import-Falle:** `config.router.ts` importiert `client`, benutzt ihn aber **nur in Funktionskörpern** (`pruefeAdmin`), nie auf Modul-Top-Level – wie `twitch.handler.ts`. (Die Falle greift hier ohnehin nicht, weil `config.router` nicht über `commands/index.js` erreichbar ist, aber die Regel wird eingehalten.) Der async **Callback ist in der Route mit `.catch()`** verkabelt (unhandled-rejection-Stolperfalle).
- **Handler sind benannt exportiert** (`requireConfigAuth`/`handleConfigPage`/`handleLogin`/`handleCallback`/`handleLogout`) → Tests rufen sie mit Mock-req/res direkt auf. `config.router.test.ts` mockt `config.json`, `client.js` (Guild/Member wie `member.handler.test.ts`) und `discordOAuth.service.js`, nutzt aber das **echte** `config.session` (mit gemocktem Secret) zum Bauen gültiger Cookies. Reine Helfer separat getestet (`config.session.test.ts`, `discordOAuth.service.test.ts` mit gemocktem `fetch`).

## Links

- [GitHub](https://github.com/denssle/mechanischer-gruener-drache)
